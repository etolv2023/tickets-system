# نظام التصميم — الأرضية الباردة والأزرق الواحد (2026-07-19)

> **الوثيقة دي بتحل محل § 6 من `CLAUDE.md`.** القسم القديم هناك بيوصف اللوحة الدافية
> السابقة واتساب زي ما هو بطلب صريح — لو لقيت تعارض بين الاتنين، **الملف ده هو الصح**.
> باقي أقسام `CLAUDE.md` (المنع من الهلوسة، التحقق اليدوي، تقسيم الملفات، الأمان،
> البيرفورمنس) سارية زي ما هي ومش بتتأثر.

---

## 1. الأطروحة

> **أي حاجة زرقا بتتداس.** الأرضية رمادي بارد هادي، ولون التفاعل الوحيد أزرق مهدّى —
> والدلالة (حالة، أولوية، نوع، لابل) **ممنوع** تكون زرقا. بكسة زرقا = وعد إن اللمس
> بيعمل حاجة (أو إن البراند بيتكلم — البورتال). مفيش استثناء تالت.

عملياً:
- زرار أساسي، لينك، فوكس رينج، الرابط النشط في السايدبار، رقم «النهاردة» في الكاليندر،
  علامة «غير مقروء»، حالة السحب فوق الرافع، الاختيار المحدد — كله `--action`.
- «جاري العمل»، «تحت الاختبار»، «متوسط» — **تيل**. اخترنا تيل مش سماوي عمداً:
  السماوي أقرب ١٥° بس لأزرق التفاعل، وفي حجم بادج ١٢px — ولمصاب عمى الألوان —
  ده مرادف مش لون تاني. التيل على بعد ٤٠°.
- `'blue'` **اتشالت من `Label::COLORS`** فالأدمن ميقدرش يخلق دلالة زرقا. الصفوف
  القديمة اتنقلت تيل بـ migration «2026_07_19_200000». توكنز `--c-blue` وكلاسات
  `.badge--blue` / `.stripe--blue` / `.cal__ticket--blue` / `.notif__icon--blue`
  عايشين **كشبكة أمان بس** لصف فات على الـ remap — متستخدمهمش في كود جديد.

## 2. السلّم الرمادي — 13 درجة باردة (عيلة ≈ 215°)

كل سطح وحد ونص هو **موضع مسمّى** على السلّم، مش hex سايب. الدارك بيعيد تعريف
الـ 13 كلهم — **مش انعكاس**: `--n-1` معناها دايماً «السطح اللي الكارت قاعد عليه»،
وعشان كده في الدارك `--n-1` أفتح من `--n-2` و`--n-3` عمداً. متحاولش «تصلّح» الترتيب.

| توكن | فاتح | غامق | | توكن | فاتح | غامق |
|---|---|---|---|---|---|---|
| `--n-0` | `#ffffff` | `#0d0f13` | | `--n-7` | `#a4adbb` | `#545c68` |
| `--n-1` | `#fafbfc` | `#262a31` | | `--n-8` | `#87909f` | `#717a89` |
| `--n-2` | `#f1f4f7` | `#1d2026` | | `--n-9` | `#5b6370` | `#9fa8b7` |
| `--n-3` | `#e7ebf1` | `#16181d` | | `--n-10` | `#454e5b` | `#c1c8d2` |
| `--n-4` | `#d9dfe7` | `#2d323a` | | `--n-11` | `#2a313b` | `#d8dce3` |
| `--n-5` | `#ccd3dd` | `#363c45` | | `--n-12` | `#161b23` | `#e8ebf0` |
| `--n-6` | `#bac2ce` | `#414855` | | | | |

## 3. الأسطح التلاتة

**ريل غاطس ← صفحة ← كارت طالع.** فاتح: ريل `--n-4`، صفحة `--n-3`، كارت `--n-1`.
غامق: ريل `--n-0`، صفحة `--n-3`، كارت `--n-1`. المسافات اللونية أقرب من اللوحة
الدافية القديمة (كارت/صفحة ≈ 1.16:1 فاتح) **عن قصد** — الظل الطبقي الجديد بياخد
جزء من شغل الفصل، فاللومينانس مبقاش لوحده.

قواعد مهمة ورثناها وسارية:
- `--surface-raised` (بوب أوفر/دروب داون) هي البيضا النقية الوحيدة في الفاتح.
- الحقول ليها توكنز نيّة مش مستوى: `--field-bg` / `--field-bg-focus` — «خطوة
  لقدام» اتجاهها مختلف في كل ثيم، والمكوّن ميعرفش الثيم.
- `--nav-hover` منفصلة لأن الهوفر جوه الريل بيتحرك ضد `--nav-bg` مش ضد `--bg`.
- الأسطح الغامقة دايماً (لايت بوكس، لوج التنصيب): `--inverse-*` و`--overlay` —
  **مبتتقلبش** في الدارك.

## 4. سلّم النص — تلات درجات، كلها AA

`--text` = `--n-12` · `--text-muted` = `--n-10` · `--text-subtle` = `--n-9`.
كلهم محسوبين ≥ 4.5:1 على `--n-1…4` فاتح و`--n-1/3/4/5` غامق (أسوأ زوج 4.52).
subtle مضمونة على السطوح الأربعة دول **بس** — متحطهاش على hover وash عابر.

## 5. الحدود والظلال

- حدود: `--border` (0.14 alpha) يحوّط السطح · `--border-subtle` (0.08/0.09) يفصل
  الصفوف جواه · `--border-strong` (0.5/0.38) **محسوبة 3:1 حقيقية** — هي التوكن
  الإجباري لأي حافة هي المؤشر الوحيد (هوفر إيدج، كومبوبوكس مفوكس). حبر الحدود
  أغمق درجة باردة، مش أسود.
- ظلال **طبقتين** (كونتاكت + أمبينت) بحبر بارد `rgb(23 32 48 / …)` مش أسود:
  `xs` تحت الكروت · `sm` هوفر والرابط النشط · `md` دروب داون/بوب أوفر ·
  `lg` **للمودال بس**.
- الحواف: `--radius-sm` 6 · `--radius` 8 (حقول/أزرار) · `--radius-lg` 12 (كروت) ·
  `--radius-xl` 16 (مودال) · `--radius-full` (pills الباقية).

## 6. عقد الـ hues — 14 للأدمن + أزرق محجوز

كل hue تلات درجات: `--c-X` (حبر) · `--c-X-bg` (خلفية) · `--c-X-line`
(**مشتقة**: `color-mix(in srgb, var(--c-X) 24%, var(--c-X-bg))` — متتكتبش بالإيد؛
الـ 24% بدل 30% القديمة لأن التينتات الباردة أفتح). كل حبر متحقق ≥ 4.5:1 على
خلفيته وعلى الكارت وعلى الصفحة في الثيمين (أسوأ زوج فاتح: تيل/صفحة 4.57).

**إضافة hue جديد = أربع أماكن:** توكنز فاتح + غامق في `tokens-color.css` /
`tokens-dark.css` (البلوكين!) · `.badge--X` · `.stripe--X` · `.cal__ticket--X` ·
وسطر في `Label::COLORS`.

**قيم الفاتح (حبر/خلفية):** red `#b23f37/#fbecea` · rose `#b03d63/#fceaf0` ·
orange `#a4511a/#faeee3` · amber `#8d5f09/#f8f0dd` · yellow `#7a680a/#f7f3da` ·
lime `#59711a/#eff4de` · green `#327349/#e9f3ec` · teal `#17766b/#e2f2ef` ·
cyan `#29708f/#e5f1f6` · blue(محجوز) `#3467a0/#e8eef7` · indigo `#4f5ab3/#ebecfa` ·
violet `#7a4fb0/#f2ebfa` · plum `#94477c/#f9e9f3` · brown `#7a5a40/#f5efe8` ·
slate `#556170/#ecf0f4`

**قيم الغامق:** red `#e88b81/#3a201e` · rose `#e289a0/#381d26` ·
orange `#de9c66/#33251a` · amber `#d9ad5a/#2f2917` · yellow `#ccb45c/#2c2a18` ·
lime `#a8c072/#232b18` · green `#74bd8b/#17291e` · teal `#5cc2b2/#122b28` ·
cyan `#6fb7d4/#142a33` · blue `#85aede/#1b2839` · indigo `#99a3e6/#20233c` ·
violet `#b596e2/#29203b` · plum `#d093bb/#33202e` · brown `#c0a188/#2e2620` ·
slate `#a2aab6/#262a30`

**الأدوار:** urgent→red · high→amber · **medium→teal** · low→slate ·
resolved→green · **progress→teal** · pending→amber · assigned→slate ·
blocked→red · bug→red · inquiry→slate · **feature→violet** · module→green.
المكوّن بيقرا الدور؛ إعادة ربط دور مبتلمسش ملف مكوّن.

## 7. التفاعل — `--action`

| توكن | فاتح | غامق |
|---|---|---|
| `--action` | `#336299` | `#6ba1e0` |
| `--action-hover` | `#2d5787` | `#82b1e8` |
| `--action-text` | `#ffffff` | `#0f1523` |
| `--action-bg` | `#e7eef6` | `#1d2f47` |

`--action-line` مشتقة بنفس قاعدة الـ 24%. `--focus: var(--action)` — الفوكس تفاعل.
الأصل المقترح `#35659c` كان 4.48:1 على الريل — اتغمّق لـ `#336299` (4.68) في
مراجعة الحساب. **متفتحهوش تاني من غير ما تعيد الحساب.**

## 8. قاعدة الحبر على الخلفيات الصلبة

فيه نوعين خلفية صلبة، ولكل واحد حبره:
- **hue مشبع ثابت** مش بيتقلب مع الثيم (مفيش حالياً غير النادر) → `--on-solid` (أبيض).
- **حبر hue متظبط بالثيم** (بادج solid، عداد الجرس، شيبس الكاليندر، زرار danger/confirm)
  → **`var(--surface)`**. السبب: حبر الدارك باستيل فاتح، والأبيض عليه ≈ 1.9:1.
  `--surface` بيقرا أبيض في الفاتح وحبر غامق على الباستيل في الغامق — AA في
  الاتنين من غير قاعدة theme-aware. أي fill جديد بحبر hue → `var(--surface)`.

## 9. لغة البادج والأشكال

- **البادج مستطيل مشطوف** (`--radius-sm`)، مش pill. التضاد الشكلي الوحيد المهم:
  **شريط الأولوية** (خط رأسي 3px) ضد **البادج** (مستطيل محدد بـ line بتاعه).
- النوع = أيقونة + بادج · الأولوية = أيقونة اتجاه + بادج + شريط · الحالة = بادج
  نصي من غير أيقونة (زي ما كان — الأسباب في § 6 القديم لسه صحيحة).
- الحالة النشطة في السايدبار: `--action-bg` + حبر `--action` + شريط علامة —
  التينت بيقول «مميز»، الشريط بيقول «انت هنا» حتى في الجريسكيل.
- التوب بار على مستوى **الصفحة** (مش كارت) بحد `--border-subtle`، أزراره ghost،
  والبحث حوض `--field-bg` من غير حد وبيرد بحلقة `--action` عند الفوكس.

## 10. ميكانيكا الثيم

- `tokens-color.css` = الفاتح (`:root`، فيه `color-scheme: light`).
- `tokens-dark.css` = **بلوكين متطابقين بايت-بايت**: `@media (prefers-color-scheme:
  dark) { :root:not([data-theme='light']) }` (بيتبع الجهاز) و`:root[data-theme='dark']`
  (لما المستخدم يختار). الاتنين لازم يفضلوا متطابقين — فحص الـ QA:
  ```
  sed -n '/^@media/,/^}/p' tokens-dark.css | sed -n '/:root:not/,/^    }/p' | sed '1d;$d' > /tmp/a
  sed -n "/^:root\[data-theme='dark'\]/,/^}/p" tokens-dark.css | sed '1d;$d' > /tmp/b
  diff /tmp/a /tmp/b   # لازم يطلع فاضي
  ```
- الدارك بيعيد تعريف: السلّم، الـ hues (حبر+خلفية)، الأسطح، النص، الحدود،
  `--action*` (بما فيهم `--action-bg` — hex مش alias)، الظلال، `color-scheme`.
  **مش** بيعيد: `-line` المشتقات، `--focus`، `--on-solid`، `--shade`، `--inverse-*`،
  `--text-inverse` — دول بيتحلوا صح لوحدهم وقت الاستخدام.
- سهم الـ `<select>` hex مضمّن في data-URI (`#7c8594`) = منتصف `--n-8` بين
  الثيمين — لو السلّم اتغيّر، اعد حسابه.

## 11. افعل / متعملش

| ✅ | ❌ |
|---|---|
| رابط نشط في السايدبار أزرق | بادج حالة أزرق |
| «غير مقروء» أزرق | «جاري العمل» أزرق — دي تيل |
| رقم النهاردة في الكاليندر أزرق (انت هنا) | stat-tile أزرق — الإحصائية دلالة، تيل |
| رد التيم في البورتال أزرق (البراند بيتكلم) | ملاحظة داخلية أزرق — دي دلالة رؤية (تيل حالياً) |
| لينك في prose أزرق | «معلومة» زرقا — الـ info alert سماوي |
| حالة سحب فوق الرافع زرقا | لون مجموعة سايدبار — الهيراركي من الموضع والوزن |

---

**التاريخ:** النظام ده حل محل اللوحة الدافية (رماديات ورقية + كروم نيلي) في
2026-07-19 بطلب إعادة تصميم كامل. البنية المعمارية اتحافظ عليها بالكامل —
السلّم الـ 13، الأسطح التلاتة، عقد التوكنز، فصل الكروم عن الدلالة — والقيم
والقاعدة الزرقا هما اللي اتغيروا.

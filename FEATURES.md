# كتالوج الفيتشرات

> **قاعدة:** لو فيتشر مش هنا → **متبنيهوش**. اسأل الأول.
> كل فيتشر ليها كود (`F00`..`F23`)، معايير قبول، والمرحلة الي بتتبني فيها.
> علامة ★ = فيتشر جديدة من طبقة Jira/الكاليندر/الاستيراد.

---

## الفكرة الأساسية

النظام ده **مزيج مقصود** بين حاجتين مبيتلمّوش مع بعض عادةً:

| الجزء | مصدره | بيجاوب على |
|---|---|---|
| **التذكرة** | نظام دعم العملاء | مين العميل؟ إيه المشكلة؟ اتحلت امتى؟ العميل اتبلغ؟ |
| **الصب تاسك** | Jira | الشغل ده بيتقسم إزاي؟ مين ياخد إيه؟ هياخد قد إيه؟ يخلص امتى؟ |

**التذكرة = الالتزام تجاه العميل. الصب تاسك = وحدة الشغل الداخلية.**

الخلط ده هو سبب وجود النظام: Jira مبيعرفش مين العميل ولا الـ SLA، ونظام التذاكر مبيعرفش يقسم الشغل ويقدّره.

---

## فهرس سريع

| الكود | الفيتشر | المرحلة |
|---|---|:--:|
| `F00` | التنصيب والمصادقة والصلاحيات | 0 |
| `F01` | الشركات وجهات الاتصال | 1 |
| `F02` | ★ استيراد Excel | 1 |
| `F03` | التذاكر — الأساس | 2 |
| `F04` | المحرر والمرفقات | 2 |
| `F05` | التعليقات والخط الزمني | 2 |
| `F06` | التوزيع وسير العمل | 3 |
| `F07` | سجل الشغل (بدأت/خلصت) | 3 |
| `F08` | ★ الصب تاسكس | 4 |
| `F09` | ★ التقدير وتتبع الوقت | 4 |
| `F10` | ★ ربط التذاكر | 4 |
| `F11` | ★ اللابلز والمتابعين | 4 |
| `F12` | البورد (Kanban) | 3 · 4 |
| `F13` | ★ الكاليندر | 5 |
| `F14` | ★ الإجازات والعطلات والسعة | 5 |
| `F15` | موافقة الفيتشرات | 3 |
| `F16` | طابور التيست | 3 |
| `F17` | التقييمات 1..10 | 6 |
| `F18` | محرك النقاط | 6 |
| `F19` | التقارير وملف الموظف | 7 |
| `F20` | الإشعارات | 4 |
| `F21` | البحث العالمي | 7 |
| `F22` | الإدارة والإعدادات | 0 · 7 |
| `F23` | سجل التدقيق | 8 |
| ★ `F27` | جيت هب — الدليل إن الشغل اتعمل | — |
| ★ `F28` | المهام المجدولة | — |
| ★ `F29` | الدخول بعين مستخدم تاني | — |

---

## F00 — التنصيب والمصادقة والصلاحيات

### F00.1 معالج التنصيب
**ليه:** تسليم المشروع لازم يكون «فك ضغط + افتح `/install` = شغال».

**السلوك:** 5 خطوات:
1. **فحص المتطلبات** — PHP ≥ 8.2 · إكستنشنز (`pdo_mysql`, `mbstring`, `fileinfo`, `gd`, `openssl`, `zip`) · صلاحيات الكتابة على `storage/` و `bootstrap/cache/`
2. **قاعدة البيانات** — host · port · **اسم الداتابيز · اليوزر · الباسورد**. اختبار الاتصال قبل الاستمرار. لو الداتابيز مش موجودة → **أنشئها**.
3. **إنشاء الجداول** — migrations + seeders (الأدوار، الصلاحيات، مصفوفة النقاط، الإعدادات الافتراضية)
4. **حساب الأدمن** — اسم · إيميل · باسورد (10 حروف على الأقل)
5. **الإنهاء** — كتابة `.env` · توليد `APP_KEY` · إنشاء `storage/installed.lock`

**معايير القبول:**
- [ ] بأدخل اسم داتابيز مش موجودة → بتتعمل لوحدها
- [ ] بأدخل باسورد غلط → رسالة خطأ واضحة، مش stack trace
- [ ] بعد التنصيب، `/install` بيرجع **404**
- [ ] `EnsureInstalled` middleware بيحوّل لـ `/install` لو `installed.lock` مش موجود
- [ ] `.env` المكتوب فيه `APP_DEBUG=false`

### F00.2 المصادقة
**معايير القبول:**
- [ ] `bcrypt` · `throttle:5,1` على الدخول · `session()->regenerate()` بعد الدخول
- [ ] كوكيز `HttpOnly` + `Secure` + `SameSite=Lax`
- [ ] انتهاء الجلسة بعد 120 دقيقة خمول
- [ ] `is_active = false` → ميقدرش يدخل، برسالة واضحة
- [ ] `must_change_password = true` (المستوردين من Excel) → إجبار تغيير قبل أي صفحة تانية

### F00.3 الأدوار والصلاحيات
**ليه:** الأدمن يعمل دور جديد بأي تركيبة من غير ما نلمس كود.

**الأدوار الافتراضية:** `admin` · `manager` · `support` · `frontend` · `backend` · `tester`
**26 صلاحية** — التفاصيل في `PLAN.md` § 2.

**معايير القبول:**
- [ ] الصلاحيات في جداول مش hardcoded
- [ ] `is_system = true` على الأدوار الأساسية → متتحذفش
- [ ] حقل `skills` (`frontend` / `backend` / `both` / `none`) بيحدد المستخدم يظهر في أنهي قائمة توزيع
- [ ] **المبرمج ممكن يكون فرونت وباك** — `skills = both` يخليه يظهر في القائمتين
- [ ] Gate بيتشيك الصلاحية، والـ Policy بتشيك ملكية الكائن

---

## F01 — الشركات وجهات الاتصال

**ليه:** التذكرة لازم تعرف مين العميل، ومين الموظف عنده الي بعت، والـ ID بتاعه على الـ ERP.

**السلوك:**
- CRUD للشركات (`name`, `code`, `is_active`, `notes`)
- CRUD لجهات الاتصال تحت كل شركة (`name`, **`erp_employee_id`**, `email`, `phone`, `is_active`)
- عند فتح تذكرة: اختار شركة → قائمة جهات الاتصال بتترشح تلقائي

**معايير القبول:**
- [ ] `UNIQUE(company_id, erp_employee_id)` — مينفعش نفس الـ ERP ID مرتين في نفس الشركة
- [ ] شركة عليها تذاكر → متتحذفش (soft delete أو منع)
- [ ] `is_active = false` → متظهرش في قوائم فتح التذاكر، بس التذاكر القديمة تفضل شغالة
- [ ] صفحة الشركة بتعرض: عدد تذاكرها · مفتوح/مقفول · متوسط زمن الحل

---

## F02 — ★ استيراد Excel

**ليه:** التيم والشركات موجودين في شيتات. مفيش حد هيدخل 200 مستخدم بإيده.

### الأنواع المدعومة
| النوع | الأعمدة |
|---|---|
| **الشركات** | `name` · `code` · `is_active` · `notes` |
| **جهات الاتصال** | `company_code` · `name` · `erp_employee_id` · `email` · `phone` · `is_active` |
| **المستخدمين** | `name` · `email` · `role_key` · `skills` · `is_active` |

### التدفق
```
1. نزّل القالب (.xlsx بأعمدة صحيحة + صف مثال + شيت تعليمات)
2. ارفع الملف
3. تحقق ومعاينة  ← الخطوة الأهم
     - بيعرض: هيتعمل كام · هيتحدّث كام · كام صف فيه خطأ
     - جدول بأول 50 صف وحالة كل واحد
     - الأخطاء بالصف والعمود: «صف 12، العمود role_key: الدور 'devops' مش موجود»
4. تأكيد → الاستيراد في queue
5. تقرير النتيجة + تحميل ملف الأخطاء (الصفوف الفاشلة بس + عمود سبب)
```

**معايير القبول:**
- [ ] **معاينة قبل الكتابة** — مفيش صف بيتكتب قبل ما أأكد
- [ ] `email` موجود قبل كده → **يتحدّث** مش يتكرر (upsert)
- [ ] **ممنوع استيراد كلمات سر من الشيت.** بيتولّد باسورد عشوائي + `must_change_password = true`
- [ ] `role_key` مش موجود → الصف يفشل برسالة واضحة، **والباقي يكمل**
- [ ] صف فاشل مبيوقفش الاستيراد كله
- [ ] حد أقصى **5000 صف** للملف
- [ ] الملف > 500 صف → بيتعالج في queue مع progress
- [ ] **حماية حقن الصيغ:** خانة بتبدأ بـ `=` `+` `-` `@` بتتعامل كنص — في الاستيراد **والتصدير**
- [ ] تحقق MIME بـ `finfo` — مش بالامتداد
- [ ] الاستيراد كله بيتسجل في `import_batches` + `activity_logs`
- [ ] `companies.manage` للشركات وجهات الاتصال · `users.manage` للمستخدمين

**نقطة دقة:** جهات الاتصال بتربط بالشركة بـ `company_code` مش `company_id` — عشان الشيت يبقى مقروء لبني آدم.

---

## F03 — التذاكر: الأساس

**السلوك:** موظف الدعم بيفتح تذكرة لكل مشكلة عميل.

**الحقول:**
| الحقل | ملاحظات |
|---|---|
| `ticket_number` | أوتو — `TK-2026-00001` |
| الشركة · جهة الاتصال | من `F01` |
| `reporter_name` · `reporter_erp_id` | **لقطة ثابتة** وقت الفتح |
| العنوان · الوصف | الوصف HTML من المحرر (`F04`) |
| **النوع** | `bug` · `inquiry` · `feature` · `new_module` · `undefined` |
| **النطاق** | `frontend` · `backend` · `both` · `inquiry` · `undefined` |
| **الأولوية** | `low` · `medium` · `high` · `urgent` |
| الموديول | نص اختياري |
| `reported_at` | **أوتو** = وقت الفتح |
| `sla_due_at` | محسوب من الأولوية |

**معايير القبول:**
- [ ] `ticket_number` متسلسل بدون فجوات وبدون تضارب تحت الضغط (استخدم transaction أو sequence table)
- [ ] `reporter_name` و `reporter_erp_id` **لقطة** — لو جهة الاتصال اتغيّرت بعدين، التذكرة القديمة تفضل صح
- [ ] تاريخ ووقت المشكلة **بيظهر أوتوماتيك** — مش بيتدخل بإيد
- [ ] الأولوية بتحسب `sla_due_at` من `settings`

### F03.1 قائمة التذاكر
**معايير القبول:**
- [ ] **الترتيب الافتراضي:** الأولوية (`urgent` → `low`) ثم **الأقدم أولاً**
- [ ] **عمود «العمر»** للمفتوحة: `now() - reported_at` بصيغة **`3 أيام و 4 ساعات`** — أحمر لو عدّى `sla_due_at`
- [ ] **عمود «زمن الحل»** للمحلولة: `resolved_at - reported_at` بنفس الصيغة
- [ ] **الفلاتر:** الحالة (محلول / غير محلول / الكل) · النوع · النطاق · الأولوية · الشركة · المسؤول · التيستر · اللابل · نطاق تاريخ · بحث نصي
- [ ] الفلاتر في الـ **query string** — اللينك يتبعت لزميلك ويفتح بنفس الفلاتر
- [ ] < 300ms مع 5000 تذكرة · مفيش N+1

---

## F04 — المحرر والمرفقات

### F04.1 محرر النصوص
- محرر (Quill أو TipTap) بدعم RTL كامل
- عريض · مائل · قوائم · لينكات · كود · اقتباس · جدول · صور inline

**معايير القبول:**
- [ ] **`Purifier::clean()` على السيرفر** قبل التخزين — مش تنقية في الجافاسكريبت
- [ ] Whitelist: `p, br, strong, em, u, ul, ol, li, a, code, pre, h3, h4, blockquote, img, table, thead, tbody, tr, td, th`
- [ ] ممنوع: `script` · `iframe` · `object` · `on*` · `style`
- [ ] لصق من Word مبيجيبش ستايل وسخ
- [ ] الاتجاه RTL افتراضي، والكود LTR

### F04.2 المرفقات
**معايير القبول:**
- [ ] رفع **أكتر من صورة** — سحب وإفلات + لصق من الكليب بورد
- [ ] Whitelist: `jpg` `jpeg` `png` `gif` `webp` `pdf` **بس**
- [ ] **5 ميجا للملف · 10 ملفات للتذكرة**
- [ ] تحقق MIME بـ `finfo` — **مش** بالامتداد ولا الـ header
- [ ] اسم `Str::uuid()` عند التخزين · الاسم الأصلي في الـ DB للعرض
- [ ] التخزين في `storage/app/private/tickets/{id}/` — **بره `public/`**
- [ ] **إعادة معالجة الصور** بـ Intervention — بيقتل أي payload مدسوس
- [ ] **صورة مصغّرة** بتتولد عند الرفع — المعرض بيعرض المصغّرة
- [ ] التحميل عن طريق راوت بيعمل `authorize()` الأول
- [ ] معرض بـ lightbox + تكبير

---

## F05 — التعليقات والخط الزمني

**السلوك:** التعليق تكملة للمشكلة — نفس المحرر ونفس الرفع.

**معايير القبول:**
- [ ] نفس المحرر ونفس قواعد التنقية بتاعة `F04`
- [ ] رفع صور على التعليق نفسه
- [ ] `is_internal` — تعليق داخلي مايتشوفش لو صدّرنا للعميل بعدين
- [ ] **الخط الزمني بيدمج زمنياً:** التعليقات + تغييرات الحالة + التوزيعات + إنشاء/إنهاء الصب تاسكس + تسجيل الوقت
- [ ] `first_response_at` بيتسجل عند أول تعليق من حد غير الي فتح التذكرة
- [ ] منشن `@اسم` → إشعار (`F20`)

---

## F06 — التوزيع وسير العمل

### الحالات
`new` · `pending_approval` · `rejected` · `assigned` · `in_progress` · `dev_done` · `testing` · `reopened` · `resolved` · `closed`

### المسارات
```
استفسار:        new → resolved → closed
بج:             new → assigned → in_progress → dev_done → testing → resolved → closed
فيتشر/موديول:   new → pending_approval → assigned → … → resolved
ارتجاع:         testing → reopened → in_progress
```

### التوزيع
- **مبرمج فرونت** (من `skills` = `frontend` أو `both`)
- **مبرمج باك** (من `skills` = `backend` أو `both`)
- **تيستر** (من دور `tester`)
- ممكن فرونت **وباك** على نفس التذكرة

**معايير القبول:**
- [ ] عند التوزيع → صف `ticket_work_logs` بيتعمل لكل جهة بحالة `pending`
- [ ] **الفيتشر متتوزعش قبل موافقة الأدمن** — الـ Policy بتمنع
- [ ] كل انتقال بيتسجل في `ticket_status_history`
- [ ] انتقال غير مسموح → exception واضح مش صمت
- [ ] **زرار «تم إبلاغ العميل»** → `client_notified_at` + `client_notified_by`
- [ ] `resolved` → `closed` بيتطلب إن العميل يكون اتبلغ

### F06.1 — ★ الحالات ديناميكية (بدل enum ثابت)

**ليه:** الأدمن يقدر يضيف حالة زي «بلوك» من غير ما حد يلمس الكود.

`ticket_statuses` (key · name_ar · color · is_system · is_open · position) +
`ticket_status_transitions` (from_status_id → to_status_id) بديل جدول
`TRANSITIONS` الثابت اللي كان جوه `TicketWorkflowService`. الـ 10 حالات
الأصلية اتزرعت `is_system = true` جوه الميجريشن نفسها (مش Seeder منفصل)
عشان `migrate` لوحدها تشتغل على قاعدة بيانات حية.

`TicketStatusValue` (`app/Casts/TicketStatusValue.php`) بديل الـ enum
`TicketStatus` القديم — object بمعرف singleton لكل key (`for()`)، بيحافظ على
نفس الشكل (`->label()`, `->variant()`, `->isOpen()`, `->value`) عشان كل
مكان بيستخدم `$ticket->status` يفضل شغال من غير تعديل.

**معايير القبول:**
- [ ] `TicketStatusDefinition::map()` / `transitionMap()` — مكاشين لحد ما حالة تتحفظ/تتحذف
- [ ] حالة `is_system` محميّة من الحذف وتغيير الـ `key` (يتفرض لما شاشة الأدمن تتبني)
- [ ] `TicketWorkflowService::transition()` بيرفض أي انتقال مش موجود في `transitionMap()`

**لسه ناقص:** شاشة أدمن CRUD للحالات والانتقالات (`/admin/ticket-statuses`) —
دلوقتي الحالات المخصصة بتتضاف يدوي (زي `DemoCustomStatusSeeder` للتجربة بس).

### F06.2 — ★ تغيير الحالة اليدوي بمستلم + صب تاسك متابعة

**ليه:** «استفسار»/«بلوك» لازم تقول مستني رد مين — تيم ولا عميل — من غير
ما تعتمد على تخمين حد إن فيه حاجة واقفة.

**السلوك:** زرار «غيّر الحالة» على التذكرة بيدّي:
- اختيار حالة من الحالات المسموح بيها من `transitionMap()` بس (مش أي حالة)
- مستلم اختياري: حد من التيم أو حد من عملاء *نفس شركة التذكرة*
- ملاحظة اختيارية

اختيار مستلم (أي نوع) بيعمل صب تاسك متابعة تلقائي — واحدة لكل جهة
(فرونت/باك) التذكرة متوزعة عليها فعلاً — **على مبرمج التذكرة نفسه، مش على
المستلم**، مستحقة النهاردة عشان تظهر في الكاليندر بتاعه فوراً. لو التذكرة
مش متوزعة لحد، مفيش صب تاسك يتعمل (مفيش حد يتحط عليه).

**معايير القبول:**
- [ ] `ticket_status_history` بيسجل `recipient_type` (`team`/`company`) +
      `recipient_user_id` أو `recipient_contact_id`
- [ ] المستلم من نوع `company` لازم يكون contact بتاع نفس شركة التذكرة
- [ ] صب تاسك المتابعة بيتعمل على مبرمج **التذكرة**، حتى لو المستلم عميل
- [ ] بدون مستلم = تغيير حالة عادي، من غير صب تاسك

### F06.3 — صب تاسك تلقائي عند أول توزيع

أول ما جهة (فرونت أو باك) تتوزع لأول مرة على تذكرة، بتتعمل صب تاسك بنفس
عنوان التذكرة، على نفس الشخص. إعادة توزيع جهة موزعة بالفعل لشخص تاني
**مبتكررش** الصب تاسك — الصب تاسك بقى ملك اللي عمله، ممكن يتنقل لحد تاني يدوي.

**معايير القبول:**
- [ ] أول توزيع لجهة (كانت `null`) → صب تاسك تلقائي بنفس العنوان
- [ ] إعادة توزيع جهة كانت متوزعة بالفعل → مفيش صب تاسك جديد
- [ ] توزيع كذا دور مرة واحدة (باك + فرونت + تستر) → صب تاسك لكل دور فيهم
- [ ] ★ نفس بلوك التوزيع متاح كمان في **صفحة إنشاء التذكرة نفسها** (اختياري)
      — نفس `assign()`، نفس سلوك الصب تاسك الأساسي.

★★ **تعديل (2026-07-23) — اليدوي بيغلب الأوتوماتيك، والموافقة بتفعّل التوزيع:**
- [ ] لو المستخدم كتب صب تاسك **يدوي** وهو بيعمل التذكرة → النظام **ماينزّلش أي
      صب تاسك أوتوماتيك** خالص (خطة المستخدم هي الخطة كلها). `seedStarters=false`
      في `assignRoles()`.
- [ ] لو التذكرة **فيتشر/موديول** (مستنية موافقة): التوزيع اللي اتاختار وقت
      الإنشاء **بيتحفظ كخطة** (`planAssignments()` — صفوف `ticket_role_assignments`
      من غير صب تاسك ولا work log ولا إشعارات ولا تغيير حالة). أول ما الأدمن
      **يوافق** → الصب تاسكات والـ work logs والإشعارات بتتنزّل والحالة تبقى
      «موزّعة» (`approve()` بيفعّل الخطة). لو كان فيه صب تاسك يدوي وقتها، الأوتو
      مايتنزّلش برضه.

---

## F07 — سجل الشغل (بدأت / خلصت)

**ليه:** ده الي بيحل **«فرونت وباك على نفس التذكرة»**.

**السلوك:** صف مستقل لكل جهة، بحالته و `started_at` و `finished_at` بتوعه.

**معايير القبول:**
- [ ] **`in_progress` مبيتحطش يدوي** — أول ما أي مبرمج يضغط «بدأت»، التذكرة تبقى `in_progress`
- [ ] **`dev_done` شرطها إن *كل* الـ work logs بحالة `done`** — لو الفرونت خلص والباك لسه، التذكرة تفضل `in_progress`
- [ ] `duration_minutes` بيتحسب عند «خلصت»
- [ ] `UNIQUE(ticket_id, side)` — جهة واحدة لكل تذكرة
- [ ] المبرمج ميقدرش يضغط «بدأت» على تذكرة مش متعيّن عليها

### ★ العلاقة بالصب تاسكس — نقطة دقة مهمة
```
ticket_work_logs  =  الالتزام على مستوى الجهة (فرونت/باك)
                     → بيحرّك حالة التذكرة + النقاط
                     → بيتعمل أوتوماتيك عند التوزيع (إجباري)

ticket_subtasks   =  تقسيم اختياري بتواريخ وتقديرات
                     → بيغذّي الكاليندر + التقدير
                     → المبرمج يعمله لو الشغل يستاهل تقسيم
```
**القاعدة الرابطة:**
- [ ] **لو الجهة عندها صب تاسكس، الـ work log بتاعها ميتحطش `done` غير لما كل صب تاسكس الجهة دي تبقى `done`**
- [ ] لو الجهة مالهاش صب تاسكس → «خلصت» بيشتغل عادي

كده مفيش تكرار: الـ work log **قرار**، والصب تاسك **تخطيط**.

### ★ الإعفاء من «خلصت» — بالزوج (2026-08-05)

**المشكلة:** «كل الجهات لازم تضغط خلصت» قاعدة صح، بس بتخلي **عادة شخص واحد**
تقدر تقفل تذكرة خلصت فعلاً. وإعفاء الشخص في كل مكان أوسع من اللازم — بيبطّل
معنى «خلصت» بتاعته خالص.

**الحل:** الإعفاء **زوج** مش خانة. الصف الواحد معناه: «فلان مش ملزم يضغط خلصت،
لما فلان التاني يكون شغال معاه على نفس التذكرة».

`worklog_completion_waivers`: `user_id` (المعفي) + `counterpart_user_id`
(**NULL = مع الكل**). بتتظبط من `/admin/users/{id}/edit`.

**معايير القبول:**
- [ ] من غير أي صف → **مفيش تغيير في السلوك خالص**
- [ ] «الشغل مع بعض» = **الاتنين ليهم صب تاسك على نفس التذكرة**، مش مجرد التوزيع
- [ ] صف باسم شخص **مش** على التذكرة → المعفي **لسه بيبلوك** ← دي الفكرة كلها
- [ ] صف بـ `counterpart_user_id = NULL` → معفي في كل مكان
- [ ] «مع الكل» بيمسح الصفوف المسماة — مصدر واحد للقاعدة
- [ ] **الربط في اتجاه واحد** — «أحمد معفي مع محمود» ≠ «محمود معفي مع أحمد»
- [ ] بيفك كمان النقلة التلقائية لـ `dev_done`
- [ ] **وبيفك بوابة الصب تاسكس كمان** — صب تاسك المعفي مابتمنعش `resolved`/`closed`
- [ ] **ساعة الحل، صب تاسكس المعفي المفتوحة بتتقفل تلقائياً** — عشان محرك النقط
      (اللي بيصرف للـ `done` بس) يشوفها ويصرفها، وعشان `LatePenaltyService`
      يبطّل يخصم منها كل صباح. **النقط بتتصرف عادي، وخصم التأخير بيتطبق عادي**
      لو كانت متأخرة — الإعفاء عن «تقول خلصت» مش عن «تسلّم في ميعادها»
- [ ] عدد اللي اتقفل بيتكتب في ملاحظة تغيير الحالة فيبان في الخط الزمني
- [ ] **حفظ الإعفاء بيعيد تقييم التذاكر العالقة فوراً** — `dev_done` حالة محسوبة
      ومبتتحسبش غير لما حد يضغط «خلصت»، فعلى تذكرة الباقي خلّصوها خلاص مكانش
      فيه أي حاجة تشغّل الفحص، وكانت بتفضل «جاري العمل» **من غير أي طريق
      لـ«تم الحل»** في القائمة أصلاً
- [ ] **البورد بيقرا نفس القاعدة** — عمود «مغلقة» بيقبل الكارت طالما السيرفر
      هيقبله. الشرط مكتوب مرة واحدة في `WorklogCompletionWaiver::waivedAmong()`
      عشان البورد والسيرفر مايختلفوش تاني
- [ ] **الـ work log مابيتقفلش نيابة عنه** — بيفضل `in_progress` في التاريخ
- [ ] **النقاط مالهاش أي علاقة** — `PointEngineService` بيصرف على الصب تاسكس
      وعمره ما بيقرا `ticket_work_logs`
- [ ] كل تغيير بيتسجل في `activity_logs`
- [ ] **⚠️ نتيجة مقصودة:** التذكرة تقدر تروح `resolved` والتيستر لسه مشافهاش

---

## F08 — ★ الصب تاسكس

**ليه:** «صلّح شاشة الفواتير» مش وحدة شغل. «عدّل الفاليديشن» + «صلّح الحساب» + «اظبط الـ PDF» — دي وحدات شغل.

**الحقول:**
| الحقل | ملاحظات |
|---|---|
| `title` | إجباري |
| `description` | اختياري — نص عادي، مش محرر (خليها بسيطة) |
| `assignee_id` | أي مستخدم |
| `side` | `frontend` · `backend` · `qa` · `support` · `other` |
| `status` | `todo` · `in_progress` · `done` · `blocked` |
| `start_date` · `due_date` | **تاريخ** (`F13` بيقرأ منهم) |
| `estimated_hours` | **التقدير** (`F09`) |
| `spent_hours` | rollup من `time_entries` |
| `blocked_reason` | لازم لو `status = blocked` |
| `position` | ترتيب السحب |
| `points` | ★ نقاط الصب تاسك نفسه — افتراضي من مصفوفة `F18`، قابل للتعديل لكل صب تاسك لوحده |

**معايير القبول:**
- [ ] بأضيف صب تاسك من جوه التذكرة — inline، من غير ما أخرج من الصفحة
- [ ] بأحدد **مسؤول · تاريخ بداية · تاريخ استحقاق · تقدير بالساعات**
- [ ] سحب وإفلات لإعادة الترتيب (`position`)
- [ ] `status = blocked` بيتطلب `blocked_reason` — مش اختياري
- [ ] عدّاد **`3/7 مكتملة`** على التذكرة + شريط تقدم
- [ ] `subtasks_total` و `subtasks_done` **عدّادات مخزّنة** — مش `COUNT()` كل عرض
- [ ] `due_date` قبل `start_date` → خطأ تحقق
- [ ] `due_date` بعد `sla_due_at` بتاع التذكرة → **تحذير** (مش منع)
- [ ] حذف تذكرة → صب تاسكس بتتحذف معاها (cascade)
- [ ] الخط الزمني بيسجل إنشاء/إسناد/إنهاء الصب تاسك
- [ ] ★ **الصب تاسك ليه نقاطه الخاصة** — مفيش نقاط على مستوى التذكرة خالص.
      الافتراضي بييجي من مصفوفة `F18` وقت الإنشاء، وبعدين قابل للتعديل لكل
      صب تاسك لوحده عادي

---

## F09 — ★ التقدير وتتبع الوقت

**ليه:** من غير تقدير، الكاليندر مجرد قائمة. ومن غير وقت فعلي، التقدير مجرد أمنية.

### التقدير
- على الصب تاسك: `estimated_hours`
- على التذكرة: `original_estimate_hours` = **يدوي** أو **rollup** من الصب تاسكس

### تسجيل الوقت — `time_entries`
`ticket_id` · `subtask_id` (اختياري) · `user_id` · `hours` · `spent_on` (تاريخ) · `note`

**معايير القبول:**
- [ ] بأسجل وقت من التذكرة أو من الصب تاسك
- [ ] `hours` بين **0.25 و 24** — تحقق
- [ ] `spent_on` مينفعش يكون في المستقبل
- [ ] `spent_hours` على الصب تاسك والتذكرة **rollup مخزّن** — بيتحدّث عند إضافة/حذف entry
- [ ] شريط **المقدّر مقابل الفعلي** على التذكرة — أخضر لو في حدود التقدير، برتقالي لو عدّاه، أحمر لو عدّى الضعف
- [ ] «الوقت المتبقي» = `estimated - spent` (مش أقل من صفر)
- [ ] بأشوف وقتي المسجّل في الأسبوع من `/my-timesheet`
- [ ] **دقة التقدير** = مقياس أداء جديد في `F19`: متوسط `estimated / actual` للشخص

**نقطة دقة:** تسجيل الوقت **مالوش علاقة بالنقاط**. النقاط على إنجاز التذكرة، مش على الساعات. عشان محدش يزوّد ساعات علشان النقط.

---

## F10 — ★ ربط التذاكر

**ليه:** «دي بتبلوك دي» و«دي نفس دي» — معلومة بتتقال في الشات وبتضيع.

**الأنواع:** `blocks` · `relates` · `duplicates` · `caused_by`

**معايير القبول:**
- [ ] بأربط تذكرة بتذكرة من صفحة التفاصيل، ببحث سريع
- [ ] الاتجاه بيتخزن **مرة واحدة**، والعرض بيوضح الاتجاهين (`blocks` ↔ `blocked_by`)
- [ ] `UNIQUE(from_ticket_id, to_ticket_id, type)` · `CHECK from != to`
- [ ] التذكرة **المبلوكة** بتتعلّم بمؤشر واضح في القائمة والبورد
- [ ] `duplicates` بيقترح إن التذكرة تتقفل — **بيمنع صرف نقاط مضاعفة على نفس الشغل**
- [ ] الصفحة الرئيسية بتوريني «تذاكر أنا بابلوكها» (`F22.1`)

---

## F11 — ★ اللابلز والمتابعين

### اللابلز
**معايير القبول:**
- [ ] `labels` بـ `name` و `color` — الأدمن بيديرها
- [ ] لابلز متعددة للتذكرة · فلتر بالابل في القائمة والبورد
- [ ] **اللون من الباليتة الدلالية بس** — مش أي لون عشوائي (شوف `CLAUDE.md` § 6)

### المتابعين
**معايير القبول:**
- [ ] «تابع» / «بطّل متابعة» من صفحة التذكرة
- [ ] المتابع بياخد إشعارات التذكرة (`F20`)
- [ ] المسؤول والتيستر والي فتحها **متابعين أوتوماتيك**
- [ ] `UNIQUE(ticket_id, user_id)`

---

## F12 — البورد (Kanban)

### F12.1 بوردي — `/my-board`
**معايير القبول:**
- [ ] 4 أعمدة: `مسندة إليّ` → `جاري العمل` → `تم التطوير` → `مغلقة`
- [ ] الكارت: رقم التذكرة · العنوان · شارة الشركة · **شريط أولوية رأسي** · شارة `فرونت`/`باك` (دوري أنا) · العمر · `3/7` صب تاسكس
- [ ] **زرار واحد سياقي:** «بدأت» → «خلصت»
- [ ] **لو عليها فرونت وباك:** مؤشر صغير بحالة الطرف التاني (`الباك: جاري العمل`) — عشان أعرف إني مستني حد
- [ ] السحب بيعمل نفس الانتقال — **بس الزرار هو الطريق الأساسي**
- [ ] تذكرة مبلوكة (`F10`) → مؤشر واضح
- [ ] `board.js` **entry منفصل في Vite** — SortableJS ميتحمّلش على صفحات تانية
- [ ] < 400ms

### F12.2 بورد التيم — `/board`
**معايير القبول:**
- [ ] نفس البورد بكل التذاكر · swimlanes حسب المسؤول أو الأولوية
- [ ] `tickets.view.all` بس

---

## F13 — ★ الكاليندر

**ليه:** التواريخ والتقديرات ملهاش قيمة لو مش شايفها على تقويم.

### العروض
`شهر` · `أسبوع` · `يوم` · `جدول زمني (Timeline)`

### المصادر
| المصدر | بيظهر بـ |
|---|---|
| صب تاسكس | `start_date` → `due_date` |
| تذاكر | `due_date` |
| مواعيد SLA | `sla_due_at` — **بشكل مميز** |
| إجازات وعطلات | `F14` |

**معايير القبول:**
- [ ] `/calendar/me` كاليندري · `/calendar/team` كاليندر التيم (`reports.view`)
- [ ] **السحب لتغيير الموعد** بيحدّث `due_date` — مع تأكيد لو هيتخطى الـ SLA
- [ ] الفلاتر: مسؤول · شركة · نوع · أولوية · لابل
- [ ] موعد SLA بيبان **مختلف** عن الصب تاسك — دايرة حمراء مش بلوك
- [ ] الصب تاسك المتأخرة (`due_date < today` و `status != done`) → أحمر
- [ ] **مؤشر السعة** لكل شخص لكل يوم (`F14`)
- [ ] `calendar.js` **entry منفصل**
- [ ] عرض الشهر < 350ms مع 2000 صب تاسك
- [ ] الأسبوع بيبدأ **السبت** (إعداد في `settings`)
- [ ] **مفيش** مكتبة كاليندر تقيلة من غير إذن — ابنيه بـ CSS Grid

---

## F14 — ★ الإجازات والعطلات والسعة

**ليه:** كاليندر بيقولك «أحمد عليه 8 ساعات النهاردة» وهو في إجازة = كاليندر بيكدب.

**معايير القبول:**
- [ ] `holidays` — تاريخ · اسم · `is_recurring` (العيد كل سنة)
- [ ] `user_leaves` — مستخدم · من · لـ · نوع (`annual`/`sick`/`other`) · `approved_by`
- [ ] الكاليندر بيعلّم الإجازات والعطلات
- [ ] **مؤشر السعة:** مجموع `estimated_hours` المستحقة في اليوم مقابل السعة اليومية (افتراضي **6 ساعات** — إعداد)
  - في الحد → رمادي · قرب الحد → كهرماني · **فوق الحد → أحمر**
- [ ] الشخص في إجازة → **سعته صفر**، وأي حاجة مسندة له اليوم ده بتتعلّم
- [ ] **حساب الـ SLA بساعات العمل** — تذكرة عاجلة اتفتحت الخميس 4 عصراً استحقاقها مش الجمعة الصبح

> **ملاحظة:** الجزء ده أضفته — مكانش في الطلب. لو هيأخر، أجّله لبعد `F13`، بس الكاليندر من غيره هيكدب.

---

## F15 — موافقة الفيتشرات

**معايير القبول:**
- [ ] النوع `feature` أو `new_module` → التذكرة تبدأ `pending_approval` أوتوماتيك
- [ ] `/approvals` طابور لصاحب `features.approve`
- [ ] الموافقة → `approval_status = approved` + `approved_by` + `approved_at` → تقدر تتوزع
- [ ] الرفض → `rejected` + سبب إجباري → **صفر نقاط للكل**
- [ ] **متتوزعش قبل الموافقة** — الـ Policy بتمنع التوزيع اليدوي من صفحة التذكرة
- [ ] **النقاط متتصرفش** غير لو `approved` (`F18`)
- [ ] ★★ التوزيع اللي اتاختار **وقت الإنشاء** بيتحفظ كخطة (`planAssignments()`) —
      مش صب تاسك ولا إشعار لحد ما يتوافق. **الموافقة بتفعّل الخطة**: صب تاسك لكل
      دور + work logs + إشعارات + الحالة «موزّعة» (`approve()` → `assign()`). راجع `F06.3`.

---

## F16 — طابور التيست

**معايير القبول:**
- [ ] `/testing-queue` — تذاكر `dev_done` أو `testing` المسندة ليّ كتيستر
- [ ] «تم التأكيد» → `resolved` + `resolved_at`
- [ ] «مرتجعة» → `reopened` + **سبب إجباري** + إشعار للمبرمجين
- [ ] `dev_done` ومفيش تيستر → الدعم أو المانجر يحلها مباشرة
- [ ] **معدل الارتجاع** مقياس جودة في `F19`

---

## F17 — التقييمات 1..10

**معايير القبول:**
- [ ] `ratings.give` بس
- [ ] خانة تقييم **لكل دور متعيّن فعلاً**: دعم · فرونت · باك · تيستر
- [ ] **مفيش فرونت متعيّن → خانة الفرونت متظهرش أصلاً** (مش disabled)
- [ ] `score` بين 1 و 10 — تحقق في الـ DB (`CHECK`) وفي الـ Request
- [ ] `UNIQUE(ticket_id, ratee_id, side)`
- [ ] تعليق اختياري مع التقييم
- [ ] **اختياري مش إجباري** — مبيعطلش الإغلاق
- [ ] التقييم بيتسجل في `activity_logs`

---

## F18 — محرك النقاط

### المصفوفة الافتراضية
| النوع | النطاق | دعم | فرونت | باك | تيستر |
|---|---|:--:|:--:|:--:|:--:|
| استفسار | `any` | 1 | — | — | — |
| بج | `frontend` | 1 | 1 | — | 0.5 |
| بج | `backend` | 1 | — | 1 | 0.5 |
| بج | `both` | 1 | 1 | 1 | 0.5 |
| فيتشر | `frontend` | 1 | 2 | — | 0.5 |
| فيتشر | `backend` | 1 | — | 2 | 0.5 |
| فيتشر | `both` | 1 | 2 | 2 | 0.5 |
| موديول جديد | `any` | 1 | 3 | 3 | 1 |

★ المصفوفة دي بقت بتخدم **الصب تاسك** بس — مش التذكرة. بتدّي القيمة
الافتراضية لنقاط الصب تاسك وقت إنشائه (`(نوع التذكرة, نطاق التذكرة, جهة
الصب تاسك)` → fallback `(نوع, any, جهة)`)، وبعدين النقطة دي ملك الصب تاسك،
تتعدل لوحدها من غير ما تلمس المصفوفة.

### شاشة المصفوفة — `/admin/point-rules`
- [ ] جدول قابل للتعديل inline · خانة رقمية + سويتش تفعيل
- [ ] الحفظ بـ AJAX + `activity_logs`
- [ ] `points.rules.manage` بس
- [ ] ★ **تصحيح يدوي** على نفس الشاشة: مستخدم · جهة · نقاط (+/-) · سبب إجباري
      · تذكرة اختيارية → سطر `point_transactions` جديد بـ `type = correction`،
      نفس صلاحية `points.rules.manage`، مسجّل في `activity_logs`

### ★★ تعديل وإلغاء التصحيح (2026-08-29)

**الطلب كان «تعديل أو حذف، وبصلاحيات منفصلة».** الصلاحيات المنفصلة اتعملت زي ما
هي؛ الحذف **اتنفّذ كسطر عكسي مش كمسح**، والقرار ده اتاخد بموافقة صريحة.

**ليه:** الدفتر ده بيبقى فلوس. لو صف اتمسح، تقرير شهر اتصرفت مكافآته لو اتطبع
تاني هيطلع رقم مختلف عن اللي اتدفع فعلاً، ومحدش هيعرف ليه. السطر العكسي بيدّي
نفس النتيجة — الرصيد مجموع، فـ`+32` و`−32` بيساووا صفر — من غير ما التاريخ
يضيع.

**اللي حصل تحت الغطا:**
- **إلغاء** → سطر واحد عكسي: نفس الشخص · نفس الدور · **نفس شهر الأصلي** ·
  النقاط بالسالب · `reverses_id` بيشاور على الأصلي
- **تعديل** → نفس السطر العكسي، **زائد** سطر بالقيمة الجديدة و`replaces_id`
  بيشاور على الأصلي. الاتنين في transaction واحدة — سطر عكسي من غير بديل جنبه
  ده خصم صامت

**`PointTransaction::booted()` ما اتلمسش.** لسه بيرمي exception على أي `update`
وأي `delete`، ومفيش استثناء اتحفر فيه — لأن «التصحيح ده اتلغى» **مش متخزّن على
الصف الأصلي**، هو **مستنتج** من وجود صف بيشاور عليه.

**معايير القبول:**
- [ ] `points.corrections.edit` و `points.corrections.delete` — **صلاحيتين
      مستقلتين**، فوق `points.rules.manage` اللي بتفتح الشاشة أصلاً. مش
      مربوطين بأي دور افتراضياً (الأدمن بياخدهم من `*`)
- [ ] الإلغاء مرتين لنفس الصف → يترفض · و`UNIQUE(reverses_id)` بيرفضه من
      الداتابيز كمان، عشان الدوسة المزدوجة ما تخصمش مرتين
- [ ] سطر الإلغاء نفسه **مبيتلغيش**
- [ ] الصرف التلقائي وخصم التأخير **مش قابلين للإلغاء من هنا** — بيتصلحوا من
      الصب تاسك نفسها
- [ ] الصف الملغي **بيفضل ظاهر** في القايمة، مشطوب وبعلامة «اتلغى» / «اتعدّل»
- [ ] كل عملية مسجّلة في `activity_logs` بالقيمة القديمة والجديدة

**حد الأمانة:** السطر العكسي بينزل على **شهر الأصلي** مش الشهر الحالي، عشان
«إلغاء» تقرا «دي ماحستش أصلاً». نتيجة كده: إلغاء تصحيح من شهر اتصرفت مكافأته
بيغيّر إجمالي الشهر ده بأثر رجعي. لو تفضّل العكس (السطر ينزل على الشهر الحالي)
ده سطر واحد في `PointCorrectionService`.

### الصرف — `PointEngineService::award()`
```
1. points_awarded_at != null           → توقف
2. feature/module و !approved          → توقف
3. هات كل صب تاسك بحالة done على التذكرة، له مسؤول ونقاطه > صفر
4. لكل صب تاسك: PointTransaction بـ subtask_id, user_id = مسؤول الصب تاسك,
   side = جهة الصب تاسك, points = points الصب تاسك نفسه (مفيش تقسيم)
5. period = resolved_at->format('Y-m')
6. points_awarded_at = now()
```

★ **مفيش مشاركين على مستوى التذكرة.** لا `created_by` ولا `assigned_frontend_id`
ولا `assigned_backend_id` ولا `tester_id` بياخدوا نقاط تلقائي — النقطة دايماً
مصدرها صب تاسك واحد `done`. لو جهة اتوزعت ومفيش عليها أي صب تاسك خلص وقت
الحل، محدش ياخد نقاط عليها (مفيش fallback). ده مضمون هيكلياً: أول توزيع لجهة
بيعمل صب تاسك أساسي تلقائي (`F06.3`)، و`F07` بيرفض «خلصت» على جهة لسه فيها صب
تاسك مفتوح — فجهة اتعمل عليها شغل فعلي دايماً بيبقى ليها صب تاسك `done` وقت
الحل.

**معايير القبول:**
- [ ] بتتنادى **مرة واحدة** عند أول دخول لـ `resolved` — الارتجاع والحل تاني **مبيديش نقاط جديدة**
- [ ] كلها جوه `DB::transaction`
- [ ] `UNIQUE(subtask_id)` — خط الدفاع الأخير: نفس الصب تاسك ميتصرفش مرتين
- [ ] **تعديل نقاط الصب تاسك بعد الصرف مبيأثرش بأثر رجعي** — المحلول يحتفظ بنقاطه
- [ ] `period` من **`resolved_at`** مش `now()`
- [ ] نفس الشخص فرونت وباك → **سطرين منفصلين**، نقاط الجهتين. مقصود.
- [ ] `rejected` → صفر للكل
- [ ] صب تاسك بجهة `other` أو بنقاط صفر أو من غير مسؤول → يتخطّى، مفيش تحذير
- [ ] الـ ledger **immutable** — التصحيح اليدوي سطر جديد (`type = correction`)، **ممنوع** UPDATE/DELETE

### ★ F18.1 — خصم التأخير (2026-08-05)

**القاعدة:** صب تاسك ليها `due_date` وعدّت التاريخ → **مبتاخدش نقاطها، بتاخدها
بالسالب.** لو عليها نقطتين تتحسب `-2`. وخلاصها بعد كده **مبيغيّرش حاجة** — مفيش
مسار في النظام شغل اتسلّم بعد ميعاده بياخد عليه نقط.

القاعدة **عمياء** عن قد إيه التأخير وعن سببه: ساعة بعد نص الليل زي أسبوعين
بالظبط، لأن الديدلاين خط مش منحدر. وصب تاسك **من غير `due_date` عمرها ما تتأخر**
— محدش وعد بحاجة، فمفيش حاجة اتكسرت.

**مين بيخصم — `points:charge-late` كل يوم ٦:٠٠ الصبح:**
```
1. هات كل صب تاسك: due_date < النهاردة · لها مسؤول · نقاطها > صفر
   · تذكرتها لسه مش محلولة · (لسه مش done) أو (done بس اتقفلت بعد آخر يوم استحقاقها)
2. استبعد اللي خلصت في ميعادها  ← TicketSubtask::finishedLate()
3. لكل واحدة: سطر point_transactions بـ type = 'penalty' و points = -نقاطها
   · charge_key = 'penalty:YYYY-MM-DD' · period = شهر الخصم · السبب مكتوب فيه
     كانت مستحقة إمتى ومتأخرة بكام يوم
```

★ **ليه كومند مش حدث؟** لأن مفيش حاجة بتحصل لما التاريخ يعدّي — لا save ولا
request ولا تغيير حالة؛ الصب تاسك بتفضل قاعدة وبتبقى متأخرة، ومفيش لحظة يتعلّق
عليها listener. والاستني لحد ما التذكرة تتحل معناه إن تذكرة مفتوحة ٣ أسابيع
متقولش لحد إنه بيخسر نقط غير يوم المكافآت — وده بالظبط الوقت اللي مبقاش ينفع
يتصرف فيه.

**★ إعداد «تراكم التأخير على التاسكات» — `/admin/settings`:**

| الحالة | السلوك |
|---|---|
| **مقفول** (الافتراضي) | خصم **واحد** لكل صب تاسك، مرة واحدة للأبد. التأخير حدث واحد بيتحاسب مرة. |
| **مفعّل** | الفحص بيخصمها **كل صباح** طول ما هي لسه متأخرة **ولسه مخلصتش**. والسبب في كل سطر بيقول إنه خصم متراكم وباليوم الي اتخصم فيه. |

★ التراكم **بيقف فوراً أول ما الصب تاسك تخلص** — من غير كده، صب تاسك خلصت
متأخرة على تذكرة طويلة كانت هتفضل تخصم من صاحبها كل يوم على شغل هو مسلّمه خلاص.

**معايير القبول:**
- [ ] `UNIQUE(subtask_id, charge_key)` — `'award'` مرة واحدة للأبد، `'penalty:تاريخ'` مرة واحدة في اليوم
- [ ] الكومند **آمن يتشغّل مرتين** في نفس اليوم — التانية بتترفض من الفهرس مش من الكود
- [ ] صب تاسك **خلصت في ميعادها** مبتتخصمش أبداً، حتى لو تاريخها بقى في الماضي من شهور
- [ ] تذكرة **محلولة** خارج نطاق الفحص — نقاطها اتقفلت وقت الحل
- [ ] فيتشر **مش متوافق عليه** لا بياخد ولا بيخسر (نفس شرط `F15`)
- [ ] `--dry-run` بيعرض الي هيتخصم من غير ما يكتب، **بنفس الكود** بتاع التشغيل الحقيقي
- [ ] `--as-of` لإعادة تشغيل صبح فات (سيرفر كان واقع) بتاريخه الصح

### ★ F18.2 — صلاحية تاريخ الاستحقاق `subtasks.schedule`

تاريخ الاستحقاق بقى **حقل فلوس** يوم ما بقى بيحدد النقط موجبة ولا سالبة —
فديدلاين اللي بيتحاسب بيه يقدر يحرّكه بنفسه **مش ديدلاين**.

- [ ] `subtasks.schedule` صلاحية مستقلة — لـ **admin + manager** بس
- [ ] بتحكم **تلات مسارات**: فورم الصب تاسك · صفوف الصب تاسك في فورم فتح التذكرة · سحب التاسك في الكاليندر
- [ ] اللي مش معاه الصلاحية **بيشوف التاريخ read-only** (مش مخفي — التاريخ حقيقة عن شغله)
- [ ] بعت `due_date` بالإيد في الريكوست؟ **يتسقّط بالسكوت**، زي `points` بالظبط
- [ ] **مش** مربوطة بـ `owns()`: هي بالذات معمولة عشان توصل لصب تاسك بتاعة حد تاني — مدير يقدر يجدول شغل فريقه، وده كل الغرض منها
- [ ] **منفصلة عن `points.rules.manage`** — جدولة الشغل وإدارة دفتر النقاط شغلانتين مختلفتين

### ★ F18.3 — سعر النقطة والمستحقات (2026-08-19)

**الفكرة:** النقط كانت دايماً من غير وحدة — الدفتر بيقول إن فلان أخد ٢، وحد
برّا النظام بيقرر الـ٢ دول يساووا كام. دلوقتي التحويل جوّا النظام.

`ticket_types.point_value` = الفلوس اللي بتاخدها النقطة الواحدة في النوع ده.

★ **مفتاحين منفصلين وده مقصود:**

| العمود | بيقول إيه | مين بيظبطه |
|---|---|---|
| `default_points` | النوع ده قد إيه شغل | اللي بيخطط |
| `point_value` | الشغل ده بيدفع كام | اللي بيدفع |

دمجهم في رقم واحد كان معناه إن المدير ميقدرش يغيّر تقدير تقل النوع من غير ما
يغيّر مرتبات الناس.

**الشاشة — `/admin/point-values`** (صلاحية `settings.manage`):
- [ ] فوق: جدول الأنواع بسعر النقطة — **فورم واحدة وحفظ واحد**، لأن كارت
      الأسعار بيتقرا عرضياً («الفيتشر ضعف البج») وحفظه صف صف بيسيبه في حالات
      محدش قصدها
- [ ] تحت: مستحقات كل شخص بالشهر، مفصّلة بالنوع (نقاط × سعر = مستحق)
- [ ] التغيير بيتسجل في `activity_logs` بـ `settings.point_values.updated`

★ **الفلوس بتتحسب وقت العرض، مش متخزنة.** الدفتر بيخزّن نقط وبس (F18 بيمنع
تعديله)، فالسعر مكانه الوحيد هو صف النوع — يعني **تغيير سعر نوع بيعيد تسعير
تاريخه كله**. وده الصح: كارت أسعار، مش عقد مجمّد.

**معايير القبول:**
- [ ] **الخصومات داخلة بالسالب** — سطر `penalty` نقاطه سالبة فبيطلع فلوس
      مخصومة. تصفيتها كانت هتدي رقم أعلى من الدفتر نفسه
- [ ] سعر **بالسالب ممنوع** (`min:0`) — سعر سالب × نقطة سالبة = مكافأة على التأخير
- [ ] الأنواع اللي سعرها صفر وعليها شغل **بتتعرض كتحذير** — إجمالي بصفر لازم
      يتقرا «الأسعار متحطتش» مش «محدش اشتغل»
- [ ] تصحيح يدوي من غير تذكرة → نوع `غير منسوبة` وسعر صفر، **بيتعرض مش بيتشال**
      عشان الأعمدة تقفل

### ★ F19.4 — فلتر النوع في `/points-report` (2026-08-19)

- [ ] دروب ليست بأنواع التذاكر جنب فلتر الشهر، **في نفس الفورم** عشان واحد
      ميضيّعش التاني
- [ ] **كل أرقام الصفحة بتتفلتر مع بعض** — الإجمالي والمقارنة بالشهر اللي فات
      والتفاصيل. مفيش رقم فاضل برّا الفلتر يناقض اللي جنبه
- [ ] جدول «النقاط حسب النوع» **بيفضل كامل** — ده الخريطة، وفلترته كانت
      هترجّعه لصف واحد المستخدم مختاره أصلاً
- [ ] نوع **مش موجود** في الـURL → يرجع لـ«كل الأنواع»، مش يفلتر للعدم
      (شهر فاضي بيتقرا حقيقة)
- [ ] الفلتر على **نوع التذكرة**، مش على عمود في الدفتر — تذكرة اتغير نوعها
      لازم تاريخها يمشي معاها

---

## F19 — التقارير وملف الموظف

### F19.1 ملف الموظف — `/employees/{user}`
اختار موظف + شهر:
- [ ] كام **بج** حل · كام **استفسار** رد عليه · كام **فيتشر** عمل · كام **موديول جديد**
- [ ] **إجمالي نقاط الشهر** + تفصيلها حسب النوع
- [ ] متوسط التقييم (1..10)
- [ ] متوسط زمن الحل
- [ ] ★ **دقة التقدير** — متوسط `estimated / actual`
- [ ] ★ **الساعات المسجّلة** في الشهر
- [ ] ★ **معدل الارتجاع** — كام تذكرة رجّعها التيستر
- [ ] جدول بكل التذاكر الي شارك فيها

### F19.2 لوحة الصدارة — `/leaderboard`
- [ ] ترتيب بنقاط الشهر · اختيار الشهر · تصدير Excel

### F19.3 باقي التقارير
- [ ] توزيع التذاكر (كام بج/استفسار/فيتشر)
- [ ] أداء الشركات (أنهي عميل بيبعت أكتر)
- [ ] متوسط زمن الحل حسب الأولوية والنوع
- [ ] خرق الـ SLA
- [ ] حِمل التيم — كل مبرمج عليه كام تذكرة مفتوحة
- [ ] ★ تقرير الوقت — مقدّر مقابل فعلي لكل شخص/شركة
- [ ] كلها تصدير `CSV` / `Excel` مع **حماية حقن الصيغ**

---

## F20 — الإشعارات

**معايير القبول:**
- [ ] جرس بعداد غير مقروء
- [ ] الأحداث: اتعملك أساين · صب تاسك اتسندت لك · التيستر رجّع · فيتشر مستنية موافقتك · منشن `@` · تذكرة بتتابعها اتغيّرت · صب تاسك متأخرة
- [ ] «علّم الكل مقروء»
- [ ] Laravel notifications — `database` channel
- [ ] الإيميل **اختياري** من الإعدادات (مقفول افتراضي)
- [ ] **ممنوع** إشعار لنفسك على فعلك أنت

### F20.2 — إشعار المتصفح بصوت *(اتضاف 2026-07-19 بطلب صريح)*

الموظف بيسيب النظام مفتوح في تاب وبيشتغل على حاجة تانية. الجرس لوحده مبيوصلش في الحالة دي.

**معايير القبول:**
- [ ] صوت مميز مستضاف محلياً (`public/sounds/`) — **مفيش CDN**، زي الخطوط
- [ ] عنوان التاب بيبقى `(3) نظام التذاكر` — بيوصل حتى لو المستخدم رافض الإشعارات
- [ ] إشعار نظام عبر `Notification API` — **الإذن يتطلب من زرار بإيماءة مستخدم**، ممنوع عند اللودة
- [ ] الضغط على الإشعار يودّي على التذكرة
- [ ] `tag` واحد — عشرة إشعارات وانت بعيد ماينفعش يبقوا عشر نوافذ
- [ ] الاستطلاع **مايقفش** لما التاب يتخفي (بوتيرة أنص)
- [ ] endpoint العدّاد **بيتخطى الكاش** — بادج متأخر دقيقة مقبول، إشعار متأخر دقيقة لأ
- [ ] لودة الصفحة **مش** وصول إشعار — مفيش رنّة على كل تنقل

> ★★★★★ **تعديل (2026-07-21):** السطر تحت ده كان بيقول "خارج النطاق صراحةً"
> — بقى غلط. Web Push + Service Worker **موجودين ومكتملين فعلاً**
> (`public/sw.js`, `app/Services/WebPushService.php`, جدول
> `push_subscriptions`, مفاتيح VAPID) — الكود سبق التوثيق من غير ما حد يحدّثه.
> بالإضافة لكده، طلب صريح من العميل إن تفعيل الإشعارات يبقى **إجباري** قد ما
> ممكن تقنياً: بانر دايم (`<x-notify-banner />`) في شِل التطبيق نفسه
> (`layouts/app.blade.php`)، ظاهر في **كل صفحة** لأي مستخدم لسه في حالة
> `default` أو `denied` — مش محصور في `/notifications` بس زي الأول. القيد
> التقني الوحيد الحقيقي فضل زي ما هو: الإذن لازم يتطلب من ضغطة مستخدم حقيقية
> (زرار)، ومفيش API يفتح إذن اترفض قبل كده تاني.
>
> - [x] Service Worker حقيقي (`public/sw.js`) — install/activate/push/notificationclick
> - [x] جدول `push_subscriptions` — اشتراك لكل جهاز، مش لكل مستخدم
> - [x] `php artisan push:vapid` — توليد مفاتيح VAPID مرة واحدة
> - [x] بانر تفعيل دايم على كل صفحة (مش زرار محصور في شاشة واحدة)
> - [x] الإشعار الحقيقي (Push) بيتبعت عبر `SendPushPing` — Job في طابور،
>       **محتاج Queue Worker شغال باستمرار على السيرفر الحقيقي**
>       (`php artisan queue:work` عبر Supervisor/systemd) — ده مش حاجة في
>       الكود، دي خطوة نشر (deployment) لازم تتأكد منها بنفسك

---

## F21 — البحث العالمي

**معايير القبول:**
- [ ] مربع واحد (`/` اختصار) بيدور في: أرقام التذاكر · العناوين · الأوصاف · التعليقات · الصب تاسكس · الشركات
- [ ] النتايج مجمّعة بالنوع
- [ ] `TK-2026-00042` → تحويل مباشر
- [ ] **بيحترم الصلاحيات** — مبيرجعش تذاكر مش من حقك
- [ ] FULLTEXT index — مش `LIKE %...%`
- [ ] < 200ms

---

## F22 — الإدارة والإعدادات

### F22.1 ★ الصفحة الرئيسية = «اليوم»
> ★★★★ **تعديل (2026-07-20):** كانت «مش لوحة أرقام» — اتلغت بطلب صريح من
> العميل. التفاصيل والسبب في `CLAUDE.md` § 6. الأربعة أقسام تحت **لسه
> إجبارية ولسه الأول في الصفحة**؛ قسم «نظرة عامة» اتضاف تحتها.

- [ ] **الي على دماغي:** صب تاسكس مستحقة النهاردة أو متأخرة
- [ ] **الي بيولّع:** خرق SLA
- [ ] **مستني قراري:** موافقات (للأدمن)
- [ ] **أنا مأخّر مين:** تذاكر أنا بابلوكها
- [ ] ★★★★ **نظرة عامة (الشهر ده):** KPI tiles (تذاكر مفتوحة · اتحلت · متوسط
      ساعات الحل · خرق SLA) + إحصائيات التذاكر بالنوع + الـ SLA بالأولوية +
      آخر التذاكر + حِمل الفريق (`reports.view` بس) + آخر نشاط (`audit.view`
      بس) — كله قراءة بس فوق `DashboardService`/`ReportService` الموجودين،
      مفيش قاعدة عمل جديدة
- [ ] **لسه ممنوع:** pie chart / donut chart — الأرقام في جداول ومقاييس، مفيش مكتبة Charts

### F22.2 الإعدادات
- [ ] اسم النظام · اللوجو
- [ ] ★ **السعة اليومية** (افتراضي 6 ساعات)
- [ ] ★ **أيام العمل** وبداية الأسبوع
- [ ] تفعيل إشعارات الإيميل
- [ ] مهل الـ SLA لكل أولوية بقت في شاشة الأولويات المستقلة — شوف F22.4

### F22.3 إدارة المستخدمين والأدوار
- [ ] CRUD + تعيين دور + `skills` + تفعيل/تعطيل
- [ ] إنشاء دور بأي تركيبة صلاحيات
- [ ] **ممنوع** حذف دور `is_system`
- [ ] إعادة تعيين باسورد → عشوائي + `must_change_password`

### F22.4 — ★ الأولويات ديناميكية (بدل enum ثابت)

**ليه:** الأدمن يقدر يضيف أولوية زي «بلوكر» من غير ما حد يلمس الكود — نفس فكرة
F06.1 بالظبط، لكن للأولوية بدل الحالة.

`priorities` (key · name_ar · color · sla_hours · is_system · position) بديل
الـ enum `Priority` القديم و`settings.sla_hours_*`. مهلة الـ SLA بقت عمود على
نفس الصف بدل ما تكون Setting منفصلة — شاشة واحدة بتعدّل الاسم واللون والمهلة
مع بعض. الـ 4 أولويات الأصلية اتزرعت `is_system = true` جوه الميجريشن نفسها
(زي الحالات بالظبط)، وبتقرا من `settings.sla_hours_*` القديمة لو موجودة
قبل ما تتمسح، عشان أي تخصيص أدمن عمله قبل كده متضيعش.

`PriorityValue` (`app/Casts/PriorityValue.php`) بديل الـ enum القديم — نفس
تصميم `TicketStatusValue` (singleton لكل key، بيحافظ على
`->label()`/`->variant()`/`->value`).

**معايير القبول:**
- [ ] `PriorityDefinition::map()` — مكاش لحد ما أولوية تتحفظ/تتحذف
- [ ] أولوية `is_system` محميّة من الحذف وتغيير الـ `key`
- [ ] حذف أولوية عليها تذاكر فعلاً → يترفض (FK RESTRICT على `tickets.priority`)
- [ ] ترتيب قائمة التذاكر الافتراضي بيقرا من `position` بتاع الأولوية، مش قائمة ثابتة في الكود
- [ ] الشرائط والبادجات بتستخدم لون الأولوية (`variant()`)، مش الـ `key` — أولوية مخصصة بلون مختلف بترسم صح

---

## F23 — سجل التدقيق

**معايير القبول:**
- [ ] `/admin/audit` بفلاتر: مستخدم · فعل · نوع الكائن · نطاق تاريخ
- [ ] بيسجل: توزيع · تغيير حالة · تقييم · تعديل قواعد نقاط · استيراد · إدارة مستخدمين · حذف
- [ ] `changes` JSON فيه قبل/بعد
- [ ] `ip` و `user_agent`
- [ ] **للقراءة بس** — مفيش تعديل ولا حذف من الواجهة

---

## F24 — ★ بوابة العميل

**ليه:** العميل يقدر يتابع تذكرته ويردّ من غير ما يحتاج لوج ولا يكلّم حد.

**السلوك:** شاشة تذكرة واحدة بس (مش بورتال كامل) على `/portal/{رقم_التذكرة}`.
كل تذكرة بتاخد باسورد عشوائي تلقائي وقت الإنشاء (`TicketService::create()` →
`Ticket::generatePortalPassword()`). **المُخزّن هو الـ hash بس** — النص
الصريح بيظهر مرة واحدة بس لحظة التوليد أو إعادة التوليد، على شاشة التذكرة
الداخلية (كارت «بوابة العميل»، محتاج صلاحية `tickets.edit`). محدش، ولا
الأدمن، يقدر يشوفه تاني — لو الموظف نساه، يولّد واحد جديد.

الدخول: رقم التذكرة + الباسورد → `POST /portal/verify` (مفيش لوج، الجلسة
بتحمل `portal_access[]` بعد النجاح). رقم التذكرة لو جاي في اللينك بيتحط في
الإنبوت **ظاهر ومش مقفول** (مش حقل مخفي)، والباسورد لازم يتكتب دايماً — مش
جزء من اللينك نفسه (تسريب أمان لو اتخزن في الهيستوري/اللوجز).

تعليق العميل بيتسجل تلقائي باسم `contact_id` بتاع التذكرة نفسها
(`TicketService::addPortalComment()`) — العميل نفسه مش مستخدم في النظام،
فمفيش `user_id` على تعليقه. `ticket_comments.user_id` بقى nullable و
`contact_id` جديد اتضاف عشانه.

**معايير القبول:**
- [ ] الباسورد بيتـhash — النص الصريح مش متخزن أبداً
- [ ] محاولات دخول غلط محدودة (10 في الدقيقة لكل رقم تذكرة + IP)، نفس رسالة الخطأ لرقم تذكرة غلط أو باسورد غلط (مايسربش إن الرقم موجود)
- [ ] العميل بيشوف بس: العنوان، الوصف، الحالة، التعليقات **العامة** (`is_internal = false`) **ومرفقاتها**
- [ ] العميل ميشوفش: التعليقات الداخلية ومرفقاتها، مرفقات التذكرة نفسها، سجل الشغل، المسؤولين، النقاط
- [ ] العميل يقدر يرفع مرفقات مع رده — راجع «رفع العميل» تحت
- [ ] تعليق العميل يظهر صح في الاتنين: البوابة والشاشة الداخلية (بشارة «عميل»)
- [ ] كل عملية إعادة توليد باسورد بتتسجل في `activity_logs`

**رفع العميل:** العميل يقدر يرفق ملفات مع رده. المرفق بيبقى مربوط بتعليقه،
ومفيش `uploaded_by` (هو مش مستخدم في النظام) — نفس سبب إن `user_id` nullable.

قاعدة الرؤية سطر واحد: **المرفق يبان للعميل لما يكون معلّق على تعليق مش
داخلي.** يعني قرار «أشارك ده مع العميل؟» هو نفسه قرار «الرد ده داخلي؟» —
مفيش سويتش تاني حد ممكن ينساه. مرفقات التذكرة نفسها بتفضل مخفية: دي مادة
شغل، مش مراسلات.

**الأمان (`config/uploads.php`):**
- قايمة أنواع أضيق من بتاعة الموظف، وحجم أقل، وعدد ملفات أقل للرسالة الواحدة
- الصور بتتعاد ترميزها → أي حمولة مدسوسة في البايتات بتموت
- **الـ PDF والفيديو مبيتعادش ترميزهم**، فمش متاحين أصلاً إلا لما `CLAMAV_ENABLED=true`
- الفحص بيحصل على الملف المؤقت **قبل** أي كتابة على الديسك
- **fail-closed:** لو الماسح مش شغال، الرفع **يترفض** — «مفحصناهوش» مش معناها «نضيف»
- تحديد معدل: 20 ملف للساعة لكل تذكرة+IP
- راوت تحميل **منفصل** عن بتاع الموظفين، بيرجع 404 لأي مرفق برة المسموح
- تنزيل دايماً `Content-Disposition: attachment` + `nosniff` + CSP sandbox

---


## ★ F26 — استقبال الاكسبشن من الأنظمة التانية (2026-08-19)

**الفكرة:** الاكسبشن اللي بيحصل في تريو (وأي سيرفر تاني) مبقاش بس رسالة على
ديسكورد وسطر في الداشبورد — بقى **تذكرة عليها اسم ومهلة**.

```
تريو: exception → Handler::report() → HandelExceptionLogs (queue)
  → ExceptionReportingService  ├─ alerts (fingerprint + count)
                               ├─ telegram / discord (زي ما هما)
                               └─ POST /api/webhooks/exceptions  ← الجديد
                                    ↓
       VerifyWebhookSignature → ExceptionIntakeService
```

### الأمان — `VerifyWebhookSignature`

مفيش سيشن ومفيش مستخدم: الطرف التاني queue worker على سيرفر تاني.
التوثيق **توقيع** بدل توكن.

- [ ] `HMAC-SHA256` على `timestamp + "\n" + raw body` — بيغطي **الجسم**
      فالتوقيع مينفعش يتشال من نداء ويتحط على تاني
- [ ] بيغطي **التوقيت** كمان — وده اللي بيخلي نافذة الـ٥ دقايق قابلة للتنفيذ
- [ ] `hash_equals` مش `==` — المقارنة اللي بتقف بدري بتسرّب التخمين بايت بايت
- [ ] **مفيش مفتاح = 503**، مش «اسمح». إنتيك من غير مفتاح باب مفتوح
- [ ] الفروق في الاتجاهين مرفوضة — توقيت في المستقبل مريب زي القديم بالظبط
- [ ] الرفض بيرجّع 401 من غير تفاصيل، والسبب الحقيقي في اللوج

### القرار — تذكرة جديدة ولا كومنت؟

الفينجربرنت هو **هوية الغلطة**، والقرار بيتحدد بحالة آخر تذكرة طلعت منه:

| حالة آخر تذكرة | اللي بيحصل |
|---|---|
| مفيش تذكرة | تذكرة جديدة |
| **لسه مفتوحة** | كومنت داخلي بعدد المرات — من غير ما يتغيّر الأساين ولا الديدلاين |
| **مقفولة/متحلّة/مرفوضة** | **تذكرة جديدة** مربوطة بالقديمة `مكررة لـ` |

★ الحالة التالتة هي المهمة: غلطة رجعت بعد ما تذكرتها اتقفلت **مش تحديث على
شغل خلص** — دي دليل إن الإصلاح مصمدش، ومحتاجة ديدلاين حي واسم من جديد.
والربط بيخلي التاريخ ماشي: تلات رجعات في تلات شهور = تلات تذاكر في سلسلة.

★ «مقفولة» = `is_open = false` من صف الحالة، **مش ليستة حالات مكتوبة في الكود**
— الأدمن بيملك الليستة دي (F06.1).

### التوزيع

- [ ] مبرمج **باك عشوائي**، و**مياخدش تنتين ورا بعض** — الاستبعاد ضد صاحب
      آخر تذكرة اكسبشن، مش ضد رُوتة متخزنة في مكان تاني
- [ ] عشوائي مش round-robin: الـround-robin محتاج مؤشر، والمؤشر بيبوظ أول ما
      حد يدخل أو يخرج أو ياخد أجازة
- [ ] **لازم يكون متاح**، والمقصود حاجتين:
      `is_active` مفعّل · **ومش في أجازة النهاردة** (`UserLeave`)
- [ ] خصم نقط من حد على ديدلاين عدّى وهو في أجازة سنوية = أسوأ حاجة ممكن
      الفيتشر ده يعملها
- [ ] شرط «مياخدش تنتين ورا بعض» **بيتساب** لو هيفضّي البركة (تيم فيه واحد
      متاح بياخد كل حاجة) — لكن شروط الإتاحة **متتسابش أبداً**؛
      «كلهم في أجازة» لازم تفشل بصوت عالي
- [ ] التوزيع بيعدّي على `TicketWorkflowService::assign()` — عشان الصب تاسك
      الأساسي والـwork log والإشعار يحصلوا زي أي توزيع عادي، ومحرك النقاط
      يشوفها تاسك عادية

### المهلة — أربع ساعات **عمل**

- [ ] بتتحسب بـ`SlaService::addWorkingHours()` — الساعة بتقف بالليل وفي
      الويك إند والأجازات الرسمية
- [ ] بتتكتب على `ticket_subtasks.due_at` (وقت مضبوط) **و** `due_date` (اليوم)
      — التاني عشان الكاليندر والبورد و«على دماغي» بيفلتروا عليه
- [ ] `points:charge-late` بقى **كل ساعة** بدل ٦:٠٠ الصبح، وإلا ديدلاين
      الساعة ٢ الضهر مكانش هيتلاحظ غير الصبح اللي بعده
- [ ] التغيير آمن لأن الخصم مفتاحه **باليوم** (`penalty:YYYY-MM-DD`) —
      الكنسة التانية في نفس اليوم بترفضها الداتابيز

### التذكرة نفسها

- [ ] العنوان `[اسم السيرفر] رسالة الغلطة` — الاكسبشن بييجي من أكتر من سيرفر،
      و«من أنهي سيرفر» أول سؤال بيحدد لو دي بتاعتك أصلاً
- [ ] الوصف فيه **كل حاجة**: السيرفر · الرابط والميثود · واجهة العميل ·
      المؤسسة · الـIP · الملف · **الـtrace كامل** · لينك للباك أوفيس
- [ ] الـtrace كامل مش لينك ليه — مبرمج محتاج يسيب التذكرة عشان يشوف الستاك
      هو مبرمج مش هيبص على الستاك
- [ ] كل قيمة بتعدّي على `e()` وبعدين `Purifier::clean()` — النص ده كله جاي
      من برّا ومحدش بيثق فيه (§ 5)
- [ ] النوع `اكسبشن` (`is_system`) · الأولوية `عاجل` · **من غير شركة** (شغل
      داخلي، F25) · `needs_approval = false` (الغلطة حصلت خلاص، مفيش حاجة
      تتوافق عليها)

### الرجوع لتريو

- [ ] الرد بيحمل `action` (`created` / `recreated` / `commented`)
      و`ticket_number` و`ticket_url`
- [ ] تريو بيخزّنهم على صف الـalert (`alerts.ticket_number` / `ticket_url`)
      فصفحة الـalert بتوصّل للشغل مش بس بتوصفه
- [ ] فشل الاتصال **مبيرميش** أبداً — مبلّغ الأخطاء اللي بيرمي وهو بيبلّغ هو
      الباج الوحيد اللي بيضخّم كل باج تاني

---

## ★ F27 — جيت هب: الدليل إن الشغل اتعمل (2026-08-29)

**ليه:** «التذكرة اتقفلت» دلوقتي معناها إن حد ضغط زرار. الفيتشر دي بتخلي معناها
إن **فيه برانش** اسمه بيبدأ برقم التذكرة، في ريبو من الأربعة. الفرق بين الاتنين
هو كل الفيتشر.

**قرار حاكم: قراءة بس.** النظام مبيعملش برانش، ولا بيفتح PR، ولا بيدمج، ولا
بيحذف. `GitHubClient` فيه **ميثود واحدة بتنادي الشبكة وهي `get()`** — مفيش كود
في التطبيق يقدر يعبّر عن كتابة. والتوكن المفروض يكون
`Contents: Read-only` + `Pull requests: Read-only` وبس، فالضمانة موجودة في
طبقتين: عندنا في الكود، وعند جيت هب في التوكن.

**الجداول:** `github_repositories` (الأربعة) · `ticket_branches` ·
`ticket_pull_requests` · `tickets.branches_count` · `users.github_login`.

> **`ticket_branches` سجل مش نسخة.** المزامنة **مبتمسحش صف أبداً**. البرانش اللي
> بيختفي من جيت هب بياخد `state = deleted` وبيفضل مكانه — لأن أغلب الوقت البرانش
> بيختفي في نفس اللحظة اللي شغله اتدمج فيها، ومسح الصف ساعتها بيمسح الدليل في
> اللحظة اللي اتكسب فيها.

### F27.1 المطابقة التلقائية

**التوليد صارم والمطابقة متسامحة**، وده مقصود: كل اسم النظام بيقترحه بيبدأ برقم
التذكرة، فـ`git branch` بيترتب بالتذكرة. لكن البرانشات اللي اتعملت بإيد قبل
النظام بتحمل بادئة Git Flow، ورفضها معناه إن شغل خلصان يتقال عنه ناقص.

**معايير القبول:**
- [ ] `TK-2026-00042` و `TK-2026-00042-anything` و `feature/TK-2026-00042`
      كلهم بيتماتشوا لنفس التذكرة
- [ ] `TK-2026-00042x` **مبيتماتشش** — لازم فاصل أو نهاية الاسم
- [ ] `team/feature/TK-2026-00042` **مبيتماتشش** — بادئة واحدة بس
- [ ] `main` و `develop` وأي برانش من غير رقم تذكرة **مبيتخزنوش** ومش بيتعاملوا
      كمشكلة
- [ ] تذكرة واحدة ليها برانش في `trioapi` وبرانش بنفس الاسم في `travel_portal_v4`
      = صفّين مستقلين. `unique(repository_id, name)` مش أي حاجة فيها `ticket_id`
- [ ] العنوان العربي **مبيتنقحرش** في الاسم المقترح — `TK-2026-00042` لوحده،
      مش `TK-2026-00042-fator-mkrr`

### F27.2 الربط اليدوي

تلات بوابات، كلها قبل أي كتابة: الاسم يماتش الاتفاقية ويسمّي **التذكرة دي** ·
**البرانش موجود فعلاً على جيت هب** (بيتسأل عنه) · مش مربوط بتذكرة تانية.

البوابة التانية هي المهمة. من غيرها ده مجرد خانة نص بتخلي أي حد يخلّي تذكرة
تبان خلصانة — وده بالظبط اللي الفيتشر موجودة تمنعه.

**معايير القبول:**
- [ ] `github.audit` مطلوبة · و`authorize('view')` على التذكرة كمان
- [ ] اسم غلط → رسالة عربية بتقول **أنهي** قاعدة اتكسرت
- [ ] برانش تذكرة تانية → «البرانش ده بتاع تذكرة تانية (TK-…)»
- [ ] برانش مش موجود → يترفض ومفيش صف بيتعمل
- [ ] التكامل مقفول → يترفض، مش بيتقبل على أساس إنه موجود
- [ ] الربط بيتسجل في `activity_logs`
- [ ] **مفيش مسار لفك الربط** — الفاليديشن بيخلي الربط الغلط شبه مستحيل،
      وإمكانية مسح الدليل بتكلّف أكتر مما تصلح

### ★★ F27.5 التغطية بالجهة، ولكل مبرمج (2026-08-29)

**«ليها برانش» مش سؤال واحد — سؤال لكل جهة.** تذكرة عليها شغل باك وفرونت
مدينة ببرانش في الاتنين. واحد من الاتنين مش «نص خلصان»، ده **غلط** — وعمود
فيه رقم بيقول «١» للتذكرة الكاملة وللمكسورة بالظبط. فالصف بقى بيوري
**شريحة لكل جهة**، والناقصة حمرا ومكتوب عليها «ناقص».

**المدين محسوب من توزيع الأدوار مش من الصب تاسكس:** التوزيع هو التزام
التذكرة، والصب تاسك خطة ممكن متكونش اتعملت. وجهة مالهاش ريبو مبتدينش
بحاجة — محدش بيعمل برانش لـ«تيستر» — فوجود تيستر على التذكرة عمره ما
يخليها تبان ناقصة. والجهة بتتغطى ببرانش في **أي** ريبو من ريبوهاتها: فيه
تلات ريبوهات فرونت، والشغل بيعيش في واحد مش في التلاتة.

**كل ده استنتاج من غير أي استعلام** — الأدوار والريبوز مكيّشين، والبرانشات
متحمّلة مع الصف.

**والشاشة بقت لكل المبرمجين.** `github.view` مش `github.audit`، لأن اللي
بيسأل «شغلي كله عليه برانش؟» هو أقدر واحد يصلّح الناقص. وده آمن لسبب واحد
بس: **`visibleTo()` هي اللي بتحدد أنهي تذاكر في النتيجة** — بنفس الـscope
بتاع `/tickets` بالظبط. المبرمج اللي معاه `tickets.view.assigned` بس بيشوف
تذاكره هو ومحدش غيره. الصلاحية عمرها ما بتبقى تجاوز لفلتر الصفوف
(CLAUDE.md § 5). زرار المزامنة فضل على `github.audit`.

**معايير القبول:**
- [ ] تذكرة عليها باك وفرونت وبرانش باك بس → **«فرونت ناقص · باك ✔»** حمرا،
      حتى وهي داخلة في فلتر «ليها برانش»
- [ ] تذكرة مفيهاش أدوار بتدين بكود → مفيش شرايح، مش تحذير
- [ ] مبرمج فرونت بيشوف تذاكره هو بس · مدير التيم بيشوف الكل
- [ ] زرار «زامن دلوقتي» بيختفي لغير `github.audit`

### F27.3 «اتقفلت من غير برانش»

**معايير القبول:**
- [ ] `github.audit` · و`visibleTo()` لسه بتحكم أنهي تذاكر تبان
- [ ] العدد ومعاه المقام («13 من 39») — رقم لوحده مبيقولش لو دي عادة ولا زلة
- [ ] بتشتغل على `tickets.branches_count` — WHERE على عمود مفهرس مش EXISTS لكل صف
- [ ] العلامة بتظهر في قايمة التذاكر وفي شريط حقائق التذكرة **من غير ما تفتح تاب**
- [ ] بتتلوّن أصفر بس على تذكرة حد قال إنها خلصت — تذكرة مفتوحة من غير برانش
      حالة عادية، ولو اتلوّنت الناس هتتعلم تتجاهل اللون

### F27.4 المزامنة

- [ ] `github:sync` كل ليلة 3 صباحاً · فشل ريبو مبيوقفش الباقي
- [ ] الخطأ بيتسجل على صف الريبو — عشان الشاشة تقول **ليه** وقف، مش بس تعرض
      تاريخ قديم
- [ ] تفاصيل الكوميت بتتطلب **بس** لما الـsha يكون اتغيّر — برانش ما اتغيرش
      مجاني للأبد
- [ ] `github:check` بيحذّر لو التوكن معاه صلاحية **كتابة** مش محتاجينها، أو
      قرّب يخلص
- [ ] التكامل مقفول → الكرون بيقول كده ويخرج بنجاح، مش بيبعت فشل كل ليلة

### مش في النطاق (لسه)

**النقاط.** الخطوة اللي بعدها — «تذكرة من غير برانش مبتاخدش نقط» — **متعملتش**
عن قصد وبطلب صريح. `PointEngineService` **ما اتلمسش خالص**. الداتا جاهزة
مستنية القرار: `github_repositories.side` من نفس مفردات `SubtaskSide`، فقاعدة
زي «الباك اند مياخدش نقط من غير برانش في ريبو باك اند» تبقى قابلة للتعبير
لما تيجي.

---
## ★ F28 — المهام المجدولة (2026-08-29)

**الطلب:** «عاوز صفحة تظهر الكرون جوبس اللي عندي وأقدر أتحكم فيهم».

**القرار الحاكم — القايمة في الكود مش في الداتابيز.** شاشة بتخليك تكتب أمر
artisan وتشغّله = **تنفيذ كود عن بُعد ورا صلاحية**. فالصف في
`scheduled_tasks` بيحمل **مفتاح** ومواعيد وسويتش، و`ScheduledTaskRegistry`
هو اللي بيقول المفتاح ده معناه أنهي أمر. **مفيش عمود `command` في الجدول
أصلاً.** إضافة مهمة = تعديل كود بيتراجع زي أي كود.

**معايير القبول:**
- [ ] `schedule.manage` — صلاحية مستقلة، **مش مربوطة بأي دور** (زي
      `system.backup`): قفل «خصم التأخير» بيوقّف الفلوس ومفيش شاشة تانية
      شكلها بيتغيّر
- [ ] المواعيد باختيارات جاهزة (كل ساعة / يوم / أسبوع / شهر) وتحتها
      «متقدم» بـ cron خام
- [ ] الـ cron المستحيل **يترفض** — `0 3 30 2 *` خمس خانات سليمة بتوصف ٣٠
      فبراير، كل بارسر بيقبلها والمهمة بعدها مبتشتغلش أبداً في صمت
- [ ] كل تشغيل بيتسجل على الصف: بدأ امتى، خلص امتى، نجح ولا لأ، أخد قد إيه
- [ ] «شغّلها دلوقتي» بتحطها في الطابور وبتلقط مخرجات الأمر
- [ ] المفتاح **بيتراجع تاني** جوه الـ job نفسه مش في الفورم بس — حمولة
      الـ job صف في جدول، وأي حاجة تقدر تكتب فيه كانت هتبقى بتدي
      `Artisan::call()` اسم أمر
- [ ] `withoutOverlapping` و `onOneServer` على كل مهمة
- [ ] الصف في الداتابيز اللي مالوش مقابل في الكود **بيتجاهل** —
      `schedule:audit` بيوري الفرق في الاتجاهين

### النبضة — الفشل اللي الشاشة كانت هتخبّيه

سيرفر متركبش عليه كرون بيوري كل مهمة بميعادها و«المرة الجاية» بتاعتها،
**ومبينفّذش ولا واحدة**، ومفيش حاجة في الصفحة شكلها غلط. عشان كده فيه
`Schedule::call` بيكتب نبضة كل دقيقة، والشاشة بتقول بالبنط العريض لو
النبضة وقفت.

النبضة **مش مهمة في القايمة** عن قصد — لو كانت، حد كان يقدر يقفلها،
وقفلها بيكسّر جرس الإنذار مش الحاجة اللي بيراقبها.

### فخ اتفادى

`routes/console.php` بيتنفّذ مع **كل** أمر artisan — بما فيهم `migrate` على
داتابيز فاضية، ونداءات المنصّب قبل ما `.env` يبقى فيه بيانات اتصال أصلاً.
فـ`ScheduledTask::activeSchedule()` **ممنوع ترمي**: `Schema::hasTable` جوه
`try`، وبترجع مصفوفة فاضية بدل ما توقّع التنصيب.

---
## ★ F29 — الدخول بعين مستخدم تاني (2026-08-29)

**الطلب:** «فيتشر لوج إن اسم الشخص عشان أقدر أشوف اللي هو بيشوفه، وتبقى صلاحية
منفصلة» — وبعدها: «عايز الأفعال تبقى باسمه، بس عايز لوج مخصوص لدا: مين دخل
باسم مين وعمل إيه».

**القرار الحاكم:** الانتحال **كامل الصلاحية** بطلب صريح — وانت داخل بعين حد
تقدر تعمل كل اللي هو يقدر يعمله، **وكل فعل بيتسجل باسمه هو**. ده مش تسريب،
ده المقصود: سجل شغل خلّصه «مبرمج باك» لازم يقرا إنه مبرمج باك على كل شاشة،
وإلا الشاشات تبدأ تكدب عن الشغل نفسه.

**التمن:** النظام بقى مش عارف مين كان على الكيبورد. `impersonation_sessions`
هو المكان اللي بيعرف — صف لكل جلسة، و`activity_logs.impersonation_id` بيربط
كل فعل اتعمل جواها بيها. فـ«مين دخل بعين مين وعمل إيه» بقى join واحد، ومحصلش
أي تزوير عشان نوصلها.

### الحواجز — كل واحد منهم بيمنع حاجة محددة

- [ ] `users.impersonate` — صلاحية مستقلة، مش مربوطة بأي دور بالإيد
- [ ] **مينفعش تنتحل حد معاه نفس الصلاحية.** من غيرها: أ ينتحل ب، ولو ب معاه
      الصلاحية يوصل لـ ج من جواها — **والسجل هيقول ب ← ج مش أ ← ج**. غسيل
      هوية وطريق مفتوح لأي حساب. النتيجة العملية إن أدمن ميقدرش ينتحل أدمن
      تاني، وده الصح مش أثر جانبي
- [ ] **مفيش تداخل** — وانت جوه جلسة، بدء جلسة تانية بيترفض
- [ ] مينفعش تنتحل نفسك ولا حساب موقوف
- [ ] `ForcePasswordChange` **بيتخطى** وانت منتحل — وإلا انتحال مستخدم مستورد
      من إكسل بيحبسك في شاشة تغيير الباسورد، والمخرج الوحيد إنك **تغيّر
      باسوردهم هما**
- [ ] الخروج **مالوش صلاحية** — وانت منتحل بقيت الشخص التاني ومعندكش
      `users.impersonate`، فلو الخروج متحرس كنت هتتقفل جوه حساب حد تاني
- [ ] السيشن بتتجدد عند الدخول وعند الخروج (منع تثبيت الجلسة)
- [ ] بانر أحمر عريض فوق كل صفحة، مايتقفلش، والخروج جواه

### السجل

- [ ] `/admin/impersonations` على `audit.view` **مش** `users.impersonate` —
      اللي بيدقّق مالوش لازمة يقدر يعمل اللي بيدققه
- [ ] عدّاد أفعال على كل جلسة (§ 4.6) — «دخل وبص» فرق عن «دخل وعمل ١٢ حاجة»
- [ ] جلسة من غير نهاية بتبان **«لسه مفتوحة»** مش بتتقفل بالتخمين — تخمين
      وقت نهاية اختراع لحقيقة عن مين عمل إيه وامتى

### التفصيلة اللي وقعت أول مرة

`ImpersonationContext` اتوصف في التعليق إنه singleton **واتنسي يتسجل كـ
singleton**. الكونتينر كان بيدي الميدل وير نسخة واللوجر نسخة تانية، فكل فعل
اتعمل وانت منتحل اتسجل بـ`impersonation_id = null`. السجل شكله كامل وضايع منه
الحاجة الوحيدة اللي اتعمل عشانها. اتكشف في التجربة مش في المراجعة —
سطر `$this->app->singleton(...)` واحد.

---
## مؤجّل عن قصد

| الفيتشر | ليه |
|---|---|
| تكامل ERP مباشر | محتاج معرفة بالـ API. `erp_employee_id` يدوي دلوقتي. |
| السبرنتات | Jira كامل. لسه مش محتاجينها. |
| الإيبكس (مستوى تالت) | تذكرة → صب تاسك كفاية. |
| تليجرام / واتساب | لما الأساس يثبت. |
| تطبيق موبايل | responsive كفاية. |
| قوالب الرد السريع | مفيدة بس مش أساسية. |

---

## الخلاصة

- **24 فيتشر** مقسّمة على **9 مراحل**.
- **الفكرة الحاكمة:** التذكرة = التزام تجاه العميل · الصب تاسك = وحدة شغل داخلية. **متخلطش بينهم.**
- **أخطر تفصيلة:** العلاقة بين `ticket_work_logs` و `ticket_subtasks` (`F07`) — الـ work log **قرار** بيحرّك الحالة والنقاط، والصب تاسك **تخطيط** بيغذّي الكاليندر والتقدير.
- **حاجات ضفتها من عندي:** الإجازات والسعة (`F14`) · دقة التقدير كمقياس (`F19`) · ربط التذاكر لمنع النقاط المضاعفة (`F10`).

# برومبت البداية

> **طريقة الاستخدام:** افتح Claude Code في مجلد `E:\etolv\jira` وابعت المحتوى الي تحت الخط.
> `CLAUDE.md` بيتحمّل أوتوماتيك — مش محتاج تلزقه.
> لو المحادثة طولت، ابدأ واحدة جديدة وقول: **«اقرا CLAUDE.md و FEATURES.md و PLAN.md، ونفّذ المرحلة X»**.

---

## المهمة

انت مهندس Laravel أول. هتبني نظام **مزيج بين Jira ونظام تذاكر دعم فني للعملاء** — عربي بالكامل وآمن.

الفكرة الحاكمة:
> **التذكرة = الالتزام تجاه العميل. الصب تاسك = وحدة الشغل الداخلية.**
>
> Jira مبيعرفش مين العميل ولا الـ SLA. نظام التذاكر مبيعرفش يقسم الشغل ويقدّره.
> احنا محتاجين الاتنين في مكان واحد — وده سبب وجود المشروع.

## اقرا الأول — بالترتيب ده

| # | الملف | ليه |
|---|---|---|
| 1 | `CLAUDE.md` | **قواعد ملزمة** — الستاك، منع الهلوسة، تقسيم الملفات، البيرفورمنس، الأمان، التصميم |
| 2 | `FEATURES.md` | **24 فيتشر** بمعايير قبول. لو حاجة مش هنا → متبنيهاش، اسأل. |
| 3 | `PLAN.md` | المعمارية · 25 جدول · 22 شاشة · 9 مراحل |

## الستاك

```
PHP 8.2+  ·  Laravel 11  ·  MySQL 8
Blade + Tailwind + Alpine.js  ·  Vite
```

**ممنوع** React / Vue / Inertia / Livewire · أي باكدج مش معتمد في `CLAUDE.md` § 1 · Repository pattern أو أي تجريد مالوش لزمة.

## أهم 6 حاجات هتغلط فيها لو مقريتش كويس

1. **`ticket_work_logs` مش `ticket_subtasks`.** الأولاني **قرار** على مستوى الجهة (فرونت/باك) بيحرّك حالة التذكرة والنقاط — بيتعمل أوتوماتيك عند التوزيع. التاني **تخطيط** اختياري بتواريخ وتقديرات بيغذّي الكاليندر. الرابط بينهم: لو الجهة عندها صب تاسكس، الـ work log بتاعها ميتحطش `done` غير لما كلها تخلص. (`FEATURES.md` § F07)

2. **ممنوع ستايل جوه الـ Blade.** مفيش `<style>` ولا `style=""`. مفيش حساء يوتيليتي في الصفحات. أي بترن اتكرر مرتين = مكون. الستايل في `resources/css/` مقسّم. (`CLAUDE.md` § 3)

3. **الصفحة الرئيسية مش داشبورد.** مفيش صف كروت إحصائيات، مفيش pie chart، مفيش «إجمالي التذاكر: 1,432». اسمها **«اليوم»** وبتجاوب على: إيه الي على دماغي، إيه الي بيولّع، إيه مستني قراري. (`CLAUDE.md` § 6)

4. **النقاط بتتصرف مرة واحدة** عند أول دخول لـ `resolved`. `UNIQUE(ticket_id, user_id, side)` هو خط الدفاع الأخير. تعديل القواعد **مبيأثرش بأثر رجعي**. (`FEATURES.md` § F18)

5. **تسجيل الوقت مالوش علاقة بالنقاط** — عشان محدش يزوّد ساعات علشان النقط.

6. **`Purifier::clean()` على السيرفر** قبل تخزين أي HTML من المحرر. و**`finfo`** للتحقق من الملفات المرفوعة، مش الامتداد.

## السرعة والتحقق — اقرا `CLAUDE.md` § 2.1

**مفيش تستات. مفيش لينترز. مفيش أدوات فحص.** لا PHPUnit ولا Pest ولا PHPStan ولا Pint.
متقترحهمش ومتركّبهمش. احنا محتاجين السيستم شغال في وقت قياسي.

**البديل: انت الـ QA.** تفتح كل شاشة بعينك، تدوس كل زرار، تجرب المسار الغلط،
تدخل بكل دور، تعدّ الاستعلامات، وتديني **تقرير تحقق** بالي شفته فعلاً — مع سطر
**«مجربتش»** صريح لأي حاجة معدتش عليها.

**ممنوع** تقول «المفروض يشتغل». شفته بعينك ولا لأ؟

## قواعد الشغل

1. **مرحلة واحدة في المرة.** نفّذ، اعرض، استنى موافقة. **متقفزش.**
2. **قبل أي كود، اعرض:**
   ```
   الملفات الي هعملها:   [قائمة]
   الملفات الي هعدّلها:   [قائمة]
   الـ migrations:       [قائمة]
   مش واضح عندي:         [قائمة أو «مفيش»]
   ```
3. **مش متأكد؟ اسأل.** متخمنش، متفترضش، متعملش حاجة «مؤقتة».
4. **بعد كل مرحلة قول:** إيه الي اتعمل · إيه أقدر أجربه دلوقتي (المسار + بيانات الدخول) · **تقرير التحقق** · إيه الي جاي.
5. تعليقات الكود إنجليزي · نصوص المستخدم عربي.

## المراحل

| # | المرحلة | المحصلة |
|:--:|---|---|
| **0** | الأساس | تنصّب، تدخل، تشوف صفحة مصممة |
| **1** | العملاء والاستيراد | بيانات حقيقية من شيتات Excel |
| **2** | التذاكر | ★ أقل منتج قابل للاستخدام |
| **3** | سير العمل | ★ التيم كله شغال |
| **4** | طبقة Jira | ★ بقى Jira مش تذاكر بس |
| **5** | الكاليندر | تخطيط بصري + سعة |
| **6** | النقاط | النقاط بتتجمع أوتوماتيك |
| **7** | التقارير | تقرير المكافآت بضغطة |
| **8** | التشديد والتسليم | zip جاهز |

التفاصيل في `PLAN.md` § 11.

---

**ابدأ: اقرا التلات ملفات، وقولّي لو فيه حاجة متعارضة أو ناقصة. بعدين اعرض خطة ملفات المرحلة 0.**

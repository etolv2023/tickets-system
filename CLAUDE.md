# قواعد المشروع — اقرأها قبل أي سطر كود

> الملف ده بيتحمّل أوتوماتيك كل جلسة. **مش اقتراحات — دي قواعد ملزمة.**
> لو حاجة هنا بتعارض طلب في المحادثة، **اسأل الأول**، متختارش لوحدك.

> ## الأولوية: السرعة
> السيستم ده محتاج يشتغل **في وقت قياسي**. مفيش تستات، مفيش لينترز، مفيش أدوات فحص —
> **انت بتجرب بإيدك** وبتديني تقرير بالي شفته (§ 2.1).
> ده **مش** إذن إنك تقول «تم» وانت مجربتش. ده العكس بالظبط:
> بدل ما التست يتحقق نيابة عنك، **انت** الي بتتحقق — وبتقولّي جربت إيه بالظبط.

---

## 0. الملفات المرجعية

| الملف | فيه إيه | امتى تفتحه |
|---|---|---|
| `CLAUDE.md` | القواعد دي | دايماً |
| `FEATURES.md` | كتالوج الفيتشرات بمعايير القبول | قبل ما تبني أي فيتشر |
| `PLAN.md` | المعمارية + نموذج البيانات + المراحل | قبل أي migration أو قرار معماري |
| `PROMPT.md` | البرومبت الأصلي | للسياق العام |

**قاعدة:** لو الفيتشر مش مكتوب في `FEATURES.md` → **متبنيهوش**. اسأل الأول.

---

## 1. الستاك — التزم بيه حرفياً

```
PHP 8.2+  ·  Laravel 11  ·  MySQL 8
Blade + Tailwind CSS + Alpine.js  ·  Vite
```

**ممنوع من غير إذن صريح:**
- أي فريمورك فرونت (React / Vue / Inertia / Livewire)
- أي باكدج جديد مش في `composer.json` أو `package.json`
- Repository pattern · CQRS · Event Sourcing · Service Locator · أي تجريد مالوش لزمة
- تغيير إصدار PHP أو Laravel

**الأصول المحلية (مش باكدجات):**
- الخطوط `Cairo` + `IBM Plex Mono` مستضافة في `public/fonts` — مفيش طلب لجوجل.
- الأيقونات SVG خام جوه `resources/views/components/icon.blade.php` — مفيش مكتبة أيقونات.

**الباكدجات المعتمدة (ودول بس):**
```
mews/purifier              تنقية HTML
intervention/image         إعادة معالجة الصور
maatwebsite/excel          استيراد وتصدير Excel
```
عايز باكدج غيرهم؟ **اسأل، واشرح ليه، واستنى موافقة.**

---

## 2. منع الهلوسة — الأهم في الملف ده

1. **متخترعش عمود.** قبل ما تكتب `$ticket->foo`، افتح الـ migration وتأكد إن `foo` موجود فعلاً.
2. **متخترعش دالة في باكدج.** لو مش متأكد إن الـ API موجود بالشكل ده، **اقرا الكود في `vendor/`** أو اسأل. متخمنش.
3. **متخترعش متطلب.** لو حاجة مش واضحة في `FEATURES.md`، **اسأل**. أسوأ حاجة تعملها إنك تفترض وتبني.
4. **متقولش «تم» من غير ما تجرب.** شوف § 2.1.
5. **متدّعيش إن حاجة شغالة عشان الـ syntax سليم.** الـ syntax السليم مش دليل على حاجة.
6. **لو غلطت، قول غلطت.** متبررش ومتخترعش سبب.
7. **متعدّلش كود مالوش علاقة بالمهمة.** شفت حاجة وحشة في ملف تاني؟ قول عليها، متصلحهاش من نفسك.

### قبل ما تكتب كود، اعرض:
```
الملفات الي هعملها:      [قائمة]
الملفات الي هعدّلها:      [قائمة]
الـ migrations:          [قائمة]
حاجة مش واضحة عندي:      [قائمة أو «مفيش»]
```
**واستنى موافقة.**

---

## 2.1 التحقق — انت الـ QA، مش الـ test suite

> **إحنا محتاجين السيستم شغال بسرعة. مش محتاجين دوامة تستات مش بتخلص.**

### ❌ ممنوع — متضيعش وقت في ده

- **متكتبش تستات.** لا PHPUnit ولا Pest ولا Feature tests ولا Unit tests. **خالص.**
- **متركّبش أدوات فحص.** لا PHPStan ولا Larastan ولا Psalm ولا Rector.
- **متركّبش لينترز كبوابة.** لا Pint ولا PHP-CS-Fixer ولا ESLint ولا Prettier.
- **متعملش مرحلة «بنية تحتية للتست».**
- **متكتبش تست يثبت البج قبل ما تصلحه.** صلّحه وجرّبه وخلاص.
- **متقترحش تستات** ولا تقول «الأفضل نضيف كوفريج». **مش هيحصل.**

### ✅ إجباري — ده بديلها

**انت الـ QA. تجرب بإيدك كل حاجة عملتها، قبل ما تقول «تم».**

قبل ما تقول مرحلة خلصت، لازم تكون عملت **كل** ده فعلاً:

1. **`php artisan migrate:fresh --seed`** — لازم تعدّي نضيفة من غير أي error.
2. **افتح كل شاشة عملتها في المتصفح.** بعينك. مش تفترض.
3. **دوس كل زرار عملته.** كل واحد.
4. **جرّب المسار الصح والمسار الغلط:**
   - فورم فاضي · بيانات غلط · تاريخ مقلوب
   - رفع ملف `.php` مغيّر امتداده لـ `.jpg` → **لازم يترفض**
   - `?id=999` لتذكرة مش بتاعتك → **لازم 403 مش 200**
5. **ادخل بكل دور بيلمس الفيتشر** وشوف كل واحد بيشوف إيه ومش بيشوف إيه.
6. **عدّ الاستعلامات في كل شاشة** (Debugbar أو `DB::listen`). **> 15 استعلام = مشكلة.**
7. **افتح كونسول المتصفح.** لازم يبقى **نضيف**. صفر errors.

### تقرير التحقق — إجباري مع كل مرحلة

```
### جربت إيه فعلاً

| الشاشة / الفعل            | الدور     | النتيجة الي شفتها       | استعلامات |
|---------------------------|-----------|-------------------------|-----------|
| /tickets                  | support   | 20 تذكرة، الفلاتر شغالة | 6         |
| /tickets                  | frontend  | تذاكري بس (3)           | 4         |
| /tickets/1 (مش بتاعته)    | frontend  | 403 ✔                   | 2         |
| رفع shell.php→jpg         | support   | اترفض ✔                 | —         |
| فورم فاضي                 | support   | رسائل خطأ عربية ✔       | —         |

migrate:fresh --seed:  عدّى نضيف
كونسول المتصفح:        نضيف
مجربتش:                [قائمة صريحة أو «مفيش»]
```

**سطر «مجربتش» ده أهم سطر في التقرير.** لو مقدرتش تجرب حاجة، **قول مجربتهاش**.
**ممنوع** تقول «المفروض يشتغل» أو «من المتوقع». شفته بعينك ولا لأ؟

### استثناء واحد بس — محرك النقاط

النقاط بتتحول لفلوس في مكافآت. غلطة فيها مش بج، دي مشكلة مع موظف.

**مش تست — سيناريو تحقق يدوي.** بعد ما تخلص `PointEngineService`:
- شغّل التذاكر الـ 8 (كل تركيبة في المصفوفة) في `tinker` أو seeder
- ورّيني **جدول `point_transactions` الناتج فعلاً**
- ورّيني إن التذكرة الي اتحلت مرتين **مأخدتش نقاط مرتين**

ورّيني الصفوف. مش تقولّي إنه شغال.

---

## 3. تقسيم الملفات

### CSS — القاعدة الي مش بتتكسر

**ممنوع نهائياً:**
```blade
{{-- ❌ ممنوع --}}
<style> .card { ... } </style>
<div style="background: red; padding: 12px">
```

**الاستثناء الوحيد** — حقن قيمة ديناميكية كـ CSS custom property:
```blade
{{-- ✅ مسموح — قيمة واحدة ديناميكية بس --}}
<div class="progress" style="--value: {{ $percent }}%">
```

### هيكل CSS
```
resources/css/
├── app.css                    ← imports بس، ولا سطر ستايل واحد
├── base/
│   ├── fonts.css              ← @font-face للخطوط المستضافة محلياً (متتعدلش بإيد — مولّدة)
│   ├── tokens.css             ← كل المتغيرات: ألوان، مسافات، مقياس خط، ظلال
│   ├── reset.css
│   ├── typography.css
│   └── rtl.css                ← إصلاحات RTL المتبقية بس
├── components/                ← ملف لكل مكون
│   ├── layout.css             ← الشل: سايدبار + توب بار + جسم بيسكرول
│   ├── nav.css
│   ├── topbar.css
│   ├── button.css
│   ├── badge.css
│   ├── card.css
│   ├── table.css
│   ├── form.css
│   ├── modal.css
│   ├── avatar.css
│   └── editor.css
└── features/                  ← ملف لكل شاشة معقدة
    ├── board.css
    ├── calendar.css
    ├── ticket-detail.css
    └── timeline.css
```

**قواعد:**
- ملف واحد = مكون واحد. **متجمعش** 5 مكونات في ملف.
- ملف عدّى **300 سطر** → قسّمه.
- كل الألوان والمسافات من `tokens.css`. **ممنوع** قيمة hex أو `px` مباشرة في ملف مكون.
- `@apply` بحساب — لو استخدمته أكتر من 6 مرات في كلاس، اكتب CSS عادي.

### Tailwind — نطاقه بالظبط

Tailwind **للتوكنز والليأوت جوه الـ Blade components بس**.

```blade
{{-- ❌ ممنوع — حساء يوتيليتي في صفحة --}}
<div class="flex items-center gap-3 rounded-lg border border-slate-200 bg-white p-4 shadow-sm hover:shadow-md transition dark:border-slate-700 dark:bg-slate-800">
  <span class="inline-flex items-center rounded-full bg-red-50 px-2 py-0.5 text-xs font-bold text-red-600">عاجل</span>
</div>

{{-- ✅ صح --}}
<x-ticket-card :ticket="$ticket">
  <x-badge variant="urgent">عاجل</x-badge>
</x-ticket-card>
```

- **صفحة Blade** (`resources/views/tickets/index.blade.php`) = ماركب دلالي + تاجات مكونات. **قرب الصفر يوتيليتي**.
- **مكون Blade** (`resources/views/components/`) = هنا اليوتيليتي مسموحة، بس لو عدّت **6 كلاسات** → انقلها لكلاس حقيقي في `resources/css/components/`.
- أي بترن اتكرر **مرتين** → يبقى مكون. من غير نقاش.

### Blade
```
resources/views/
├── layouts/
│   ├── app.blade.php          ← الشِل بس: html, head, nav, main slot
│   ├── auth.blade.php
│   └── install.blade.php
├── components/                ← مكونات قابلة لإعادة الاستخدام
│   ├── badge.blade.php
│   ├── avatar.blade.php
│   ├── ticket-card.blade.php
│   ├── priority-stripe.blade.php
│   └── ...
├── partials/                  ← أجزاء خاصة بشاشة
│   └── tickets/
│       ├── _filters.blade.php
│       ├── _sidebar.blade.php
│       └── _subtasks.blade.php
└── tickets/
    ├── index.blade.php
    ├── create.blade.php
    └── show.blade.php
```

- **ملف Blade عدّى 150 سطر → قسّمه** لـ partials أو مكونات.
- **ممنوع** منطق أعمال في Blade. مفيش `@php` بلوكات فيها حسابات. الحساب في الـ Controller أو الـ Model accessor.
- **ممنوع** استعلام جوه `@foreach`. لو محتاج علاقة، `with()` من الكونترولر.

### JavaScript
```
resources/js/
├── app.js                     ← entry: Alpine + مكونات عامة بس
├── components/
│   ├── editor.js
│   ├── uploader.js
│   └── datepicker.js
└── features/
    ├── board.js               ← entry مستقل — تحميل في صفحة البورد بس
    └── calendar.js            ← entry مستقل — تحميل في الكاليندر بس
```
البورد والكاليندر **entries منفصلة في Vite**. متحمّلش SortableJS على صفحة الدخول.

### PHP
```
app/
├── Enums/                     TicketStatus, TicketType, Priority, WorkSide, SubtaskStatus...
├── Http/
│   ├── Controllers/           نحيفة — تحقق، نادي Service، ارجع View
│   ├── Middleware/
│   └── Requests/              Form Request لكل POST/PUT. من غير استثناء.
├── Models/                    علاقات + scopes + accessors بس
├── Policies/                  Policy لكل موديل
└── Services/                  ← كل منطق الأعمال هنا
    ├── TicketWorkflowService.php
    ├── SubtaskService.php
    ├── PointEngineService.php
    ├── TimeTrackingService.php
    ├── CalendarService.php
    ├── ImportService.php
    ├── AttachmentService.php
    └── ReportService.php
```
- **كونترولر عدّى 150 سطر أو ميثود عدّت 25 سطر → المنطق مكانه Service.**
- Service بتاخد وترجع بيانات. **متلمسش** `request()` ولا `session()` ولا `auth()` جواها — مرّرهم كـ parameters.

---

## 4. البيرفورمنس

### إلزامي
1. **`Model::preventLazyLoading()`** في `AppServiceProvider` لما `!app()->isProduction()`. أي N+1 يرمي exception في التطوير.
2. **`with()` دايماً** على أي استعلام قائمة. متسيبش العلاقة تتحمّل في اللوب.
3. **`select()` الأعمدة المطلوبة بس** في القوائم. متجيبش `description` (LONGTEXT) في شاشة فيها 50 تذكرة.
4. **Pagination على كل قائمة.** `simplePaginate()` لو مش محتاج العدد الكلي.
5. **فهرس على كل FK** وكل عمود في `WHERE` أو `ORDER BY`.
6. **عدّادات مسبقة الحساب** بدل `COUNT()` كل مرة:
   - `tickets.subtasks_total` / `tickets.subtasks_done`
   - `tickets.spent_hours` (rollup من `time_entries`)
   - `point_transactions.period` (محسوب مش `DATE_FORMAT` وقت التقرير)
7. **كاش للبيانات الي مبتتغيرش:** `settings` و `point_rules` و `permissions`.
   `Cache::rememberForever()` + بطّل الكاش في الـ model `saved` event.
8. **صور مصغّرة عند الرفع.** متعرضش صورة 4MB في معرض التذكرة.
9. **الاستيراد في queue.** ملف 3000 صف ميتعالجش في request.

### أهداف قابلة للقياس
| الشاشة | الهدف | بكام تذكرة |
|---|---|---|
| قائمة التذاكر | < 300ms | 5,000 |
| البورد | < 400ms | 5,000 |
| الكاليندر (شهر) | < 350ms | 2,000 صب تاسك |
| تفاصيل التذكرة | < 250ms | 50 تعليق |
| التقرير الشهري | < 800ms | 20,000 معاملة نقاط |

**قبل ما تقول مرحلة خلصت:** شغّل Debugbar أو `DB::listen`، وقولّي كل شاشة بتعمل كام استعلام. **أكتر من 15 استعلام في صفحة = مشكلة.**

---

## 5. الأمان — مفيش تفاوض

| البند | القاعدة |
|---|---|
| HTML من المحرر | `Purifier::clean()` **على السيرفر** قبل التخزين. ممنوع `{!! !!}` على محتوى مستخدم من غير تنقية. |
| رفع الملفات | تحقق MIME بـ `finfo` مش بالامتداد · تخزين في `storage/app/private/` · اسم `Str::uuid()` · إعادة معالجة بـ Intervention |
| التحميل | راوت بيعمل `authorize()` الأول · `Content-Disposition: attachment` · `X-Content-Type-Options: nosniff` |
| IDOR | Policy لكل موديل · **ممنوع** `Model::find()` من غير `authorize()` بعدها · Query scopes حسب الدور |
| SQL | Eloquent بس · **ممنوع** `DB::raw()` بمدخلات مستخدم |
| Mass assignment | `$fillable` على كل موديل · **ممنوع** `$guarded = []` |
| التحقق | Form Request لكل POST/PUT. من غير استثناء. |
| كلمات السر | `bcrypt` · **ممنوع** md5/sha1 · `throttle:5,1` على الدخول |
| CSRF | على كل POST/PUT/DELETE. متشيلوش. |
| استيراد Excel | نظّف مدخلات الصيغ — أي خانة بتبدأ بـ `=` `+` `-` `@` تتعامل كنص |
| تصدير Excel | نفس الحماية — **CSV injection** خطر حقيقي |
| الاستيراد | **ممنوع** استيراد كلمات سر من الشيت. ولّد عشوائي + إجبار تغيير أول دخول. |

**كل تعديل حساس يتسجل في `activity_logs`:** التوزيع، تغيير الحالة، التقييم، تعديل قواعد النقاط، الاستيراد، إدارة المستخدمين.

---

## 6. التصميم — مش داشبورد تقليدي

### الأطروحة
> **الكروم بقى بلون براند واحد (نيلي/indigo) — والبادجات الدلالية (أولوية/حالة/نوع/SLA) لسه ليها لوحتها المستقلة.**

★ **تعديل (2026-07):** القاعدة الأصلية كانت «مفيش لون زخرفي خالص، الكروم رمادي بالكامل». اتغيّرت بطلب صريح — الكروم (أيقونات السايدبار، الأزرار الأساسية، `nav__brand-mark`) بقى بياخد لون براند واحد ثابت (`--action` = نيلي/indigo)، عشان الواجهة متبقاش عايمة رمادي في رمادي. **اخترنا indigo مش blue عمداً** — الأزرق (`--c-blue`) محجوز فعلاً لـ«متوسط/جاري العمل» على البادجات، فلو الزرار الأساسي اتلوّن بنفس الأزرق كان هيتلخبط بصرياً مع بادج الحالة. **الخط الفاصل لسه موجود:** لون الكروم/التفاعل (`--action`) حاجة، ولون الدلالة (أولوية، حالة، نوع، SLA) حاجة تانية — كل واحد ليه الـ hue بتاعه، مفيش تداخل.

★★ **تعديل (2026-07-18) — السُلَّم:** التشخيص كان إن الواجهة «عايمة، كله في بعضه». السبب مكانش التنفيذ، كان إن النظام كان بيدي **درجتين بس** لكل لون (حبر + خلفية) و٤ أسطح رمادية. درجتين مش بيوصّفوا هيراركي. الي اتغيّر:

- **سُلَّم رمادي دافي من ١٣ درجة** (`--n-0` … `--n-12`) بقى العمود الفقري. كل سطح وحد ونص هو **موضع مسمّى على السلّم** مش hex سايب — يعني «درجة أفتح» بقت حركة حقيقية ومتكررة.
- **كل hue بقى ٣ درجات** مش اتنين: `--c-X-bg` (الخلفية) · `--c-X-line` (الحد، **مشتقّة بـ `color-mix` مش مكتوبة بالإيد**) · `--c-X` (الحبر). درجة الحد بتتظبط لوحدها في الثيمين لأن مدخلاتها الاتنين متظبطة أصلاً.
- **الأبيض النقي اتشال من `--surface`.** بقى `--n-1` (`#fbfaf9`). الأبيض ١٠٠٪ على خلفية رمادية دافية مصدر وهج، وعلى شاشة مبنية كروت هو أغلب الشاشة.
- **الدارك اترفع من قاع الأسود.** كان `--bg: #111110` والسطح فوقه بفرق ٦٪ بس — مفيش مكان تحت الكارت، فالكروت بطّلت تتقري كأسطح. بقى `#1a1a18` وكل درجة فوقه ليها مساحة حقيقية.
- **`color-scheme` اتضاف على `:root`.** من غيره ويندوز بيرسم قايمة الـ `<select>` المنبثقة والسكرول بار **بالأبيض حتى في الثيم الداكن**. دي كانت السبب الحقيقي لشكل الدروب ليست الوحش، مش أي حاجة في `form.css`.
- **حقول الإدخال بقى ليها توكنز بالنية مش بالمستوى:** `--field-bg` / `--field-bg-focus`. السبب إن «خطوة لقدّام» اتجاه مختلف في كل ثيم، و`--surface` أغمق من `--surface-2` في الدارك — فلو المكوّن قال `--surface` كان الفوكس هيغمّق الحقل بدل ما يفتحه.

★★ **تعديل (2026-07-18b) — الأسطح التلاتة:** السايدبار كان `--n-2`، يعني **أفتح** من الصفحة بدرجة واحدة صغيرة — فمكانش ليه مستوى، وكان قدّام المحتوى اللي بيأطّره بدل ما يكون وراه.

**دلوقتي التكديس تلات مستويات صريحة:** الريل (`--nav-bg` = `--n-4` فاتح / `--n-0` غامق، **غاطس**) → الصفحة (`--bg`) → الكارت (`--surface`، **طالع**). الرابط النشط بيقفز لمستوى الكارت كامل، فبيبقى الحاجة الوحيدة في العمود اللي قدّام. و`--nav-hover` موجود لأن الهوفر جوه الريل لازم يتحرك ضد `--nav-bg` مش ضد `--bg`.

★★★ **إلغاء — ألوان مجموعات السايدبار:** القاعدة القديمة كانت «كل مجموعة في السايدبار تاخد hue مختلف من العشرة الإضافيين». **اتلغت.** سبع درجات مشبعة في عمود ٢١٠px بتبقى قوس قزح، وبتصرف لون على **التنقل** — الجزء الوحيد في التطبيق الي مفيهوش حاجة عاجلة أبداً — فالخمسة الدلاليين بقوا بيزعّقوا فوقه. **دلوقتي: الهيراركي من الموضع والوزن، ولون السايدبار الوحيد هو الصفحة الحالية** (`--action` + شريط علامة)، لأن دي المعلومة الوحيدة في القايمة.

### الصفحة الرئيسية = «اليوم»

★★★★ **تعديل (2026-07-20) — إلغاء «مفيش لوحة أرقام»:** القاعدة الأصلية هنا كانت
تمنع أي كروت إحصائيات أو أرقام إجمالية على الصفحة الرئيسية خالص («الأرقام
مكانها `/reports` بس»). **اتلغت بطلب صريح من العميل** — عايز شكل SaaS تقليدي
فيه KPI cards من أول ما يفتح النظام. الصفحة دلوقتي فيها **قسمين**:

1. **الأربعة أقسام الأصلية (لسه زي ما هي، ولسه الأول في الصفحة):** «على
   دماغي/بيولّع/مستني قراري/أنا مأخّر مين» — شغل مستحق، مش عدّادات. الترتيب
   ده مقصود: الشغل الي المستخدم لازم يتحرك بسببه لسه قبل أي رقم.
2. **★★★★ قسم نظرة عامة (جديد، تحت الأربعة أقسام):** KPI tiles (تذاكر مفتوحة
   دلوقتي، اتحلت الشهر ده، متوسط ساعات الحل، خرق SLA الشهر ده) + إحصائيات
   التذاكر بالنوع + نظرة على الـ SLA بالأولوية + آخر التذاكر + حِمل الفريق +
   آخر نشاط. كله **قراءة بس** فوق `ReportService`/`DashboardService`
   الموجودين أصلاً — مفيش قاعدة عمل جديدة، ومفيش رقم بيتحسب هنا مش موجود في
   `/reports` أصلاً. كل ويدجت محكوم بنفس صلاحية الشاشة الكاملة بتاعته (حِمل
   الفريق وآخر نشاط، مثلاً، مش ظاهرين غير لمين عنده `reports.view`/`audit.view`
   أصلاً) — الصفحة الرئيسية ما بتكشفش حاجة زيادة عن الي المستخدم يقدر يوصلها
   من مكان تاني.

**لسه ممنوع** (القواعد دي ماتغيّرتش):
- Pie chart / Donut chart — **مفيش، وهيفضل مفيش.** الأرقام في جداول ومقاييس (meters)، مش رسوم دائرية. مفيش مكتبة Charts اتضافت.
- Hero بـ gradient
- Glassmorphism · ظلال تقيلة على الكروت (الظل للمودال بس)
- إيموجي كأيقونات أقسام — الأيقونات SVG من `<x-icon>`
- سايدبار تخين بأكورديون متداخل
- كروت كبيرة مليانة هوا

**✅ الصفحة الرئيسية تجاوب على:**
1. إيه الي على دماغي دلوقتي؟ (صب تاسكس مستحقة النهاردة)
2. إيه الي بيولّع؟ (خرق SLA)
3. إيه الي مستني قراري؟ (موافقات — للأدمن)
4. إيه الي أنا مأخّر بيه حد؟ (تذاكر بتبلوك ناس)
5. ★★★★ إيه الوضع العام الشهر ده؟ (نظرة عامة — KPIs + إحصائيات + حِمل الفريق)

**تفاصيل التقارير الكاملة (بفلتر شهر، تصدير Excel...) لسه في `/reports`** — الصفحة الرئيسية بتدّي نظرة سريعة بس، مش بديل عن `/reports`.

### اللغة البصرية
| العنصر | القاعدة |
|---|---|
| الكثافة | Linear-like. صفوف ضيقة، معلومات حقيقية. الهوا في الوصف بس. |
| الخطوط | عربي: **Cairo** (مستضاف محلياً في `public/fonts`) · بيانات وأرقام: **IBM Plex Mono** |
| الأرقام | `font-variant-numeric: tabular-nums` في **كل** جدول. كل رقم/كود/تاريخ ISO مونوسبيس. |
| الأولوية | شريط رأسي 3px على جنب الكارت |
| الحالة | شارة نصية (pill) — **شكل مختلف** عن الأولوية عشان ميتخانقوش |
| الحواف | `--radius-sm` 5px · `--radius` 9px (حقول وأزرار) · `--radius-lg` 13px (كروت) · `--radius-xl` 15px (مودال) · `--radius-full` (pills) |
| الفواصل | `--border` يحوّط السطح · `--border-subtle` يفصل الصفوف **جوه** السطح — خطوط شعرة `1px`، مش ظلال |
| RTL | logical properties (`ms-`, `me-`, `ps-`, `pe-`, `inset-inline-start`). **ممنوع** `left`/`right` |
| الوضع الليلي | من اليوم الأول. توكنز على `:root`، إعادة تعريف في dark. |
| الحركة | بحساب. hover و transitions قصيرة بس. **ممنوع** scroll animations. |
| `prefers-reduced-motion` | محترم دايماً |

### الألوان الدلالية

كل درجة معاها خلفيتها، والاتنين متظبطين بالإيد لكل ثيم. كلهم من نفس العيلة: حبر مهدّى متوسط الغمق على خلفية دافية باهتة.

```css
/* الخمسة الأساسية — دي الي الأدوار بتتربط عليها */
--c-red:   #b4433a  /  --c-red-bg:   #faeae7    عاجل · تخطّى SLA · مرتجعة
--c-amber: #a9720f  /  --c-amber-bg: #f7efe0    مرتفع · تم التطوير · بانتظار الموافقة
--c-blue:  #3a6ea5  /  --c-blue-bg:  #e9eef5    جاري العمل · متوسط · اليوم الحالي
--c-green: #3d7a52  /  --c-green-bg: #eef4ef    تم الحل · خلصت · في حدود السعة
--c-slate: #5b6570  /  --c-slate-bg: #eef0f2    مسندة · منخفض · محايد

/* عشرة زيادة — متاحة للأدمن يختار منها لحالة أو أولوية أو لابل بيعملها */
--c-rose  --c-orange  --c-yellow  --c-lime  --c-teal
--c-cyan  --c-indigo  --c-violet  --c-plum  --c-brown
```

فوقهم فيه **أسماء أدوار** (`--c-urgent`, `--c-bug`, `--c-resolved`…) كلها aliases. المكوّنات بتقرا الدور، والدرجة تفصيلة تنفيذ — فتغيير ربط دور مبيلمسش أي ملف مكوّن.

**الرماديات دافية** (`#f2f0ec` خلفية · `#1c1b19` نص) — مش مائلة للأزرق ومش محايدة.

**`Label::COLORS` هي اللوحة الوحيدة** الي الأدمن بيختار منها. أي لون جديد فيها لازم يتضاف في **تلات أماكن**: توكن `--c-<key>` و `--c-<key>-bg` (فاتح + غامق) في `tokens.css` · `.badge--<key>` · `.stripe--<key>`. درجة `--c-<key>-line` **متكتبش بالإيد** — سطر `color-mix` واحد جنب إخواته.

**الكروم بيتلوّن في مكان واحد بس:**
**العناصر التفاعلية** — زرار أساسي، `nav__brand-mark`، فوكس رينج، والرابط النشط في السايدبار — `--action` (indigo) دايماً، مفيش استثناء.

**برة دي، الكروم رمادي.** جدول التذاكر، الكروت، الفورمز، السايدبار كله ما عدا الصفحة الحالية — كل ده رمادي دافي، واللون فيه دلالي بس (أولوية، حالة، نوع، SLA).

### الأيقونات — إجبارية على النوع والأولوية

بادج النوع كان **كلمة ملوّنة** بس، يعني تفرّق بج من استفسار لازم تقرا. ومع إن نوعين ممكن يشتركوا في نفس الـ hue (بج أحمر، مرتجعة أحمر)، اللون لوحده مش كفاية.

| العنصر | الشكل | المصدر |
|---|---|---|
| النوع | أيقونة + بادج | `TicketType::icon()` |
| الأولوية | أيقونة اتجاه (`priority-urgent/high/medium/low`) + بادج + شريط | `PriorityValue::icon()` |
| الحالة | بادج نصي، **من غير أيقونة** | — |

- أيقونة الأولوية **اتجاه مش لون ولا عدد شرايط** — عشان تفضل مقروءة في الجريسكيل ولعمى الألوان، والشريط أصلاً شايل الـ hue.
- `PriorityValue::icon()` بتشتغل بـ **ترتيب الصف في القايمة (`position`) مش بالمفتاح** — عشان أي أولوية يخترعها الأدمن («حرج»، «مؤجل») تاخد السهم الصح.
- الحالة فضلت من غير أيقونة **عمداً**: تلات pills بأيقونات في صف واحد بتلغي بعض. النوع أخد الأيقونة، الحالة أخدت الشكل.

### الموبايل — قواعد ملزمة

النظام بيتشغّل من الفون بشكل أساسي، مش حالة استثنائية.

1. **السايدبار درج (drawer) تحت `60rem`، مش شريط فوق المحتوى.** قبل كده كان بيتحوّل لصف بيلف — سبع مجموعات = حوالي **١١٠٠ بكسل من القايمة فوق كل صفحة**. الحالة `data-nav-open` على `<html>`، و**متتخزنش** في `localStorage`: طبقة بتغطي الشاشة ممنوع تكون هي أول حاجة تقابلك في اللودة الجاية.
   - الدرج بينزلق بـ `translate` **مش** بـ `inset-inline-start` — القيمة المنطقية مبتتعادش حسابها بعد ما العنصر يبقى `position: fixed`، حتى لو حطيتها inline، والدرج بيفتح بره الشاشة. `translate` فيزيائي فالاتجاه بيتحدّد بـ `:dir()`.
   - زرار `.topbar__toggle` **لازم يفضل ظاهر** على الموبايل — هو المقبض الوحيد للدرج.
2. **أي حاجة بتكسر جوتر الجسم لازم تنفي `--body-pad`** (المنشور في `layout.css`) مش `--space-5` مباشرة. الجوتر بيصغّر على الفون، فرقم ثابت بيخلي العنصر يطلّع برة الصفحة ويجيب سكرول أفقي.
3. **أي جدول أوسع من الشاشة يسكرول جوه نفسه** (`overflow-x: auto` + `min-width` على الصف)، مش يزقّ الصفحة. ورقة الوقت (تذكرة × ٧ أيام) مثال — تسع أعمدة مستحيل تتحشر في 375px.
4. **المقياس:** `document.documentElement.scrollWidth == innerWidth`. أي زيادة = بج.
5. **`100vh` ممنوعة لوحدها** — دايماً `100dvh` وراها، عشان شريط عنوان المتصفح اللي بيطبق.

### الشاشات المنفصلة عن الشِل

`layouts/portal.blade.php` و `layouts/auth.blade.php` **مالهمش توب بار ولا ناف**. أي كنترول عام (زرار الثيم) لازم يتحط فيهم بإيدك — مكوّن `<x-theme-toggle />` مشترك بين التلاتة عشان ميتكتبش تلات مرات.

**الأيقونة:** `public/favicon.svg` — الرسمة الافتراضية. **مش** أول حرف من اسم النظام، لأنها بتبوظ أول ما الأدمن يغيّر الاسم. لوجو مرفوع (`app_logo`) بيكسبها في كل layout.

> ⚠️ **فخ في Blade:** متكتبش تاج مكوّن (`<x-…>`) جوه أي تعليق في ملف مكوّن — ولا حتى جوه `/* */` أو بين علامات تنصيص. Blade بيمسح الملف الخام ويدوّر على التاجات **قبل** ما يفسّر `@props`، فتاج في تعليق بيتحوّل لنداء render في نص المصفوفة وبيوقّع الصفحة بـ `Undefined variable $component`.

### قواعد ملزمة للـ CSS
- **ممنوع** hex أو rgb مباشرة في أي ملف مكوّن. كله من التوكنز.
- **ممنوع** قاعدة `[data-theme='dark']` في ملف مكوّن. الوضع الليلي إعادة تعريف توكنز في `tokens.css` وبس — لو كتبت قاعدة dark يبقى استخدمت توكن غلط.
- **ممنوع** `left`/`right`. logical properties دايماً.
- الأسطح الغامقة دايماً (لايت بوكس، لوج التنصيب) ليها توكنز خاصة: `--inverse-surface` · `--inverse-text` · `--overlay` — ودي **مبتتقلبش** في الدارك.

---

## 7. قواعد الشغل

1. **مرحلة واحدة في المرة.** نفّذ، اعرض، استنى موافقة. **متقفزش** للي بعدها.
2. **اعرض قائمة الملفات قبل ما تكتب.**
3. **كل جدول = migration مستقلة** باسم واضح.
4. **بعد كل مرحلة قول بالظبط:**
   - إيه الي اتعمل
   - إيه الي أقدر أجربه دلوقتي (المسار + بيانات الدخول)
   - **تقرير التحقق** (§ 2.1) — إجباري
   - إيه الي جاي
5. **تعليقات الكود بالإنجليزي. نصوص المستخدم بالعربي.**
6. **مفيش TODO في الكود المسلّم.** لو حاجة ناقصة، قول عليها في الرد.
7. **Seeder ببيانات واقعية** مع كل مرحلة — عشان أقدر أجرب من غير ما أدخل بيانات بإيدي.

---

## 8. لما تكون مش متأكد

```
اسأل.
```

مش:
- تخمن وتكمل
- تعمل حاجة «مؤقتة» تصلحها بعدين
- تبني الاتنين وتخليني أختار
- تفترض الافتراضي المعقول

**اسأل. سؤال واحد واضح أرخص من مرحلة كاملة غلط.**
